# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under AnimationNode. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.AnimationNode

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^AnimationNode = def^{
	self^{ abstract^gdobj : godot.AnimationNode },

	override^new = f^obj:godot.AnimationNode {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNode",

	delegate^self^.gdobj
}

public^let^AnimationNodeSync = def^{
	self^{ abstract^gdobj : godot.AnimationNodeSync },

	override^new = f^obj:godot.AnimationNodeSync {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeSync",

	delegate^self^.gdobj
}

public^let^AnimationNodeAdd2 = def^{
	self^{ abstract^gdobj : godot.AnimationNodeAdd2 },

	override^new = f^obj:godot.AnimationNodeAdd2 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeAdd2",

	delegate^self^.gdobj
}

public^let^AnimationNodeAdd3 = def^{
	self^{ abstract^gdobj : godot.AnimationNodeAdd3 },

	override^new = f^obj:godot.AnimationNodeAdd3 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeAdd3",

	delegate^self^.gdobj
}

public^let^AnimationRootNode = def^{
	self^{ abstract^gdobj : godot.AnimationRootNode },

	override^new = f^obj:godot.AnimationRootNode {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationRootNode",

	delegate^self^.gdobj
}

public^let^AnimationNodeAnimation = def^{
	self^{ abstract^gdobj : godot.AnimationNodeAnimation },

	override^new = f^obj:godot.AnimationNodeAnimation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeAnimation",

	delegate^self^.gdobj
}

public^let^AnimationNodeBlend2 = def^{
	self^{ abstract^gdobj : godot.AnimationNodeBlend2 },

	override^new = f^obj:godot.AnimationNodeBlend2 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeBlend2",

	delegate^self^.gdobj
}

public^let^AnimationNodeBlend3 = def^{
	self^{ abstract^gdobj : godot.AnimationNodeBlend3 },

	override^new = f^obj:godot.AnimationNodeBlend3 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeBlend3",

	delegate^self^.gdobj
}

public^let^AnimationNodeBlendSpace1D = def^{
	self^{ abstract^gdobj : godot.AnimationNodeBlendSpace1D },

	override^new = f^obj:godot.AnimationNodeBlendSpace1D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeBlendSpace1D",

	delegate^self^.gdobj
}

public^let^AnimationNodeBlendSpace2D = def^{
	self^{ abstract^gdobj : godot.AnimationNodeBlendSpace2D },

	override^new = f^obj:godot.AnimationNodeBlendSpace2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeBlendSpace2D",

	delegate^self^.gdobj
}

public^let^AnimationNodeBlendTree = def^{
	self^{ abstract^gdobj : godot.AnimationNodeBlendTree },

	override^new = f^obj:godot.AnimationNodeBlendTree {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeBlendTree",

	delegate^self^.gdobj
}

public^let^AnimationNodeExtension = def^{
	self^{ abstract^gdobj : godot.AnimationNodeExtension },

	override^new = f^obj:godot.AnimationNodeExtension {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeExtension",

	delegate^self^.gdobj
}

public^let^AnimationNodeOneShot = def^{
	self^{ abstract^gdobj : godot.AnimationNodeOneShot },

	override^new = f^obj:godot.AnimationNodeOneShot {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeOneShot",

	delegate^self^.gdobj
}

public^let^AnimationNodeOutput = def^{
	self^{ abstract^gdobj : godot.AnimationNodeOutput },

	override^new = f^obj:godot.AnimationNodeOutput {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeOutput",

	delegate^self^.gdobj
}

public^let^AnimationNodeStateMachine = def^{
	self^{ abstract^gdobj : godot.AnimationNodeStateMachine },

	override^new = f^obj:godot.AnimationNodeStateMachine {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeStateMachine",

	delegate^self^.gdobj
}

public^let^AnimationNodeSub2 = def^{
	self^{ abstract^gdobj : godot.AnimationNodeSub2 },

	override^new = f^obj:godot.AnimationNodeSub2 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeSub2",

	delegate^self^.gdobj
}

public^let^AnimationNodeTimeScale = def^{
	self^{ abstract^gdobj : godot.AnimationNodeTimeScale },

	override^new = f^obj:godot.AnimationNodeTimeScale {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeTimeScale",

	delegate^self^.gdobj
}

public^let^AnimationNodeTimeSeek = def^{
	self^{ abstract^gdobj : godot.AnimationNodeTimeSeek },

	override^new = f^obj:godot.AnimationNodeTimeSeek {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeTimeSeek",

	delegate^self^.gdobj
}

public^let^AnimationNodeTransition = def^{
	self^{ abstract^gdobj : godot.AnimationNodeTransition },

	override^new = f^obj:godot.AnimationNodeTransition {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeTransition",

	delegate^self^.gdobj
}
