# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Resource. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Resource

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^Animation = def^{
	self^{ abstract^gdobj : godot.Animation },

	override^new = f^obj:godot.Animation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Animation",

	delegate^self^.gdobj
}

public^let^AnimationLibrary = def^{
	self^{ abstract^gdobj : godot.AnimationLibrary },

	override^new = f^obj:godot.AnimationLibrary {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationLibrary",

	delegate^self^.gdobj
}

public^let^AnimationNodeStateMachinePlayback = def^{
	self^{ abstract^gdobj : godot.AnimationNodeStateMachinePlayback },

	override^new = f^obj:godot.AnimationNodeStateMachinePlayback {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeStateMachinePlayback",

	delegate^self^.gdobj
}

public^let^AnimationNodeStateMachineTransition = def^{
	self^{ abstract^gdobj : godot.AnimationNodeStateMachineTransition },

	override^new = f^obj:godot.AnimationNodeStateMachineTransition {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationNodeStateMachineTransition",

	delegate^self^.gdobj
}

public^let^Occluder3D = def^{
	self^{ abstract^gdobj : godot.Occluder3D },

	override^new = f^obj:godot.Occluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Occluder3D",

	delegate^self^.gdobj
}

public^let^ArrayOccluder3D = def^{
	self^{ abstract^gdobj : godot.ArrayOccluder3D },

	override^new = f^obj:godot.ArrayOccluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ArrayOccluder3D",

	delegate^self^.gdobj
}

public^let^AudioBusLayout = def^{
	self^{ abstract^gdobj : godot.AudioBusLayout },

	override^new = f^obj:godot.AudioBusLayout {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioBusLayout",

	delegate^self^.gdobj
}

public^let^BitMap = def^{
	self^{ abstract^gdobj : godot.BitMap },

	override^new = f^obj:godot.BitMap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BitMap",

	delegate^self^.gdobj
}

public^let^BoneMap = def^{
	self^{ abstract^gdobj : godot.BoneMap },

	override^new = f^obj:godot.BoneMap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoneMap",

	delegate^self^.gdobj
}

public^let^BoxOccluder3D = def^{
	self^{ abstract^gdobj : godot.BoxOccluder3D },

	override^new = f^obj:godot.BoxOccluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoxOccluder3D",

	delegate^self^.gdobj
}

public^let^Shape3D = def^{
	self^{ abstract^gdobj : godot.Shape3D },

	override^new = f^obj:godot.Shape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Shape3D",

	delegate^self^.gdobj
}

public^let^BoxShape3D = def^{
	self^{ abstract^gdobj : godot.BoxShape3D },

	override^new = f^obj:godot.BoxShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoxShape3D",

	delegate^self^.gdobj
}

public^let^ButtonGroup = def^{
	self^{ abstract^gdobj : godot.ButtonGroup },

	override^new = f^obj:godot.ButtonGroup {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ButtonGroup",

	delegate^self^.gdobj
}

public^let^CameraAttributes = def^{
	self^{ abstract^gdobj : godot.CameraAttributes },

	override^new = f^obj:godot.CameraAttributes {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CameraAttributes",

	delegate^self^.gdobj
}

public^let^CameraAttributesPhysical = def^{
	self^{ abstract^gdobj : godot.CameraAttributesPhysical },

	override^new = f^obj:godot.CameraAttributesPhysical {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CameraAttributesPhysical",

	delegate^self^.gdobj
}

public^let^CameraAttributesPractical = def^{
	self^{ abstract^gdobj : godot.CameraAttributesPractical },

	override^new = f^obj:godot.CameraAttributesPractical {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CameraAttributesPractical",

	delegate^self^.gdobj
}

public^let^Shape2D = def^{
	self^{ abstract^gdobj : godot.Shape2D },

	override^new = f^obj:godot.Shape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Shape2D",

	delegate^self^.gdobj
}

public^let^CapsuleShape2D = def^{
	self^{ abstract^gdobj : godot.CapsuleShape2D },

	override^new = f^obj:godot.CapsuleShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CapsuleShape2D",

	delegate^self^.gdobj
}

public^let^CapsuleShape3D = def^{
	self^{ abstract^gdobj : godot.CapsuleShape3D },

	override^new = f^obj:godot.CapsuleShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CapsuleShape3D",

	delegate^self^.gdobj
}

public^let^CircleShape2D = def^{
	self^{ abstract^gdobj : godot.CircleShape2D },

	override^new = f^obj:godot.CircleShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CircleShape2D",

	delegate^self^.gdobj
}

public^let^SyntaxHighlighter = def^{
	self^{ abstract^gdobj : godot.SyntaxHighlighter },

	override^new = f^obj:godot.SyntaxHighlighter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SyntaxHighlighter",

	delegate^self^.gdobj
}

public^let^CodeHighlighter = def^{
	self^{ abstract^gdobj : godot.CodeHighlighter },

	override^new = f^obj:godot.CodeHighlighter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CodeHighlighter",

	delegate^self^.gdobj
}

public^let^ColorPalette = def^{
	self^{ abstract^gdobj : godot.ColorPalette },

	override^new = f^obj:godot.ColorPalette {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ColorPalette",

	delegate^self^.gdobj
}

public^let^Compositor = def^{
	self^{ abstract^gdobj : godot.Compositor },

	override^new = f^obj:godot.Compositor {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Compositor",

	delegate^self^.gdobj
}

public^let^CompositorEffect = def^{
	self^{ abstract^gdobj : godot.CompositorEffect },

	override^new = f^obj:godot.CompositorEffect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompositorEffect",

	delegate^self^.gdobj
}

public^let^ConcavePolygonShape2D = def^{
	self^{ abstract^gdobj : godot.ConcavePolygonShape2D },

	override^new = f^obj:godot.ConcavePolygonShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConcavePolygonShape2D",

	delegate^self^.gdobj
}

public^let^ConcavePolygonShape3D = def^{
	self^{ abstract^gdobj : godot.ConcavePolygonShape3D },

	override^new = f^obj:godot.ConcavePolygonShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConcavePolygonShape3D",

	delegate^self^.gdobj
}

public^let^ConvexPolygonShape2D = def^{
	self^{ abstract^gdobj : godot.ConvexPolygonShape2D },

	override^new = f^obj:godot.ConvexPolygonShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConvexPolygonShape2D",

	delegate^self^.gdobj
}

public^let^ConvexPolygonShape3D = def^{
	self^{ abstract^gdobj : godot.ConvexPolygonShape3D },

	override^new = f^obj:godot.ConvexPolygonShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConvexPolygonShape3D",

	delegate^self^.gdobj
}

public^let^CryptoKey = def^{
	self^{ abstract^gdobj : godot.CryptoKey },

	override^new = f^obj:godot.CryptoKey {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CryptoKey",

	delegate^self^.gdobj
}

public^let^Curve = def^{
	self^{ abstract^gdobj : godot.Curve },

	override^new = f^obj:godot.Curve {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Curve",

	delegate^self^.gdobj
}

public^let^Curve2D = def^{
	self^{ abstract^gdobj : godot.Curve2D },

	override^new = f^obj:godot.Curve2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Curve2D",

	delegate^self^.gdobj
}

public^let^Curve3D = def^{
	self^{ abstract^gdobj : godot.Curve3D },

	override^new = f^obj:godot.Curve3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Curve3D",

	delegate^self^.gdobj
}

public^let^CylinderShape3D = def^{
	self^{ abstract^gdobj : godot.CylinderShape3D },

	override^new = f^obj:godot.CylinderShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CylinderShape3D",

	delegate^self^.gdobj
}

public^let^Environment = def^{
	self^{ abstract^gdobj : godot.Environment },

	override^new = f^obj:godot.Environment {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Environment",

	delegate^self^.gdobj
}

public^let^GLTFDocument = def^{
	self^{ abstract^gdobj : godot.GLTFDocument },

	override^new = f^obj:godot.GLTFDocument {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFDocument",

	delegate^self^.gdobj
}

public^let^FBXDocument = def^{
	self^{ abstract^gdobj : godot.FBXDocument },

	override^new = f^obj:godot.FBXDocument {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FBXDocument",

	delegate^self^.gdobj
}

public^let^GLTFState = def^{
	self^{ abstract^gdobj : godot.GLTFState },

	override^new = f^obj:godot.GLTFState {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFState",

	delegate^self^.gdobj
}

public^let^FBXState = def^{
	self^{ abstract^gdobj : godot.FBXState },

	override^new = f^obj:godot.FBXState {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FBXState",

	delegate^self^.gdobj
}

public^let^Noise = def^{
	self^{ abstract^gdobj : godot.Noise },

	override^new = f^obj:godot.Noise {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Noise",

	delegate^self^.gdobj
}

public^let^FastNoiseLite = def^{
	self^{ abstract^gdobj : godot.FastNoiseLite },

	override^new = f^obj:godot.FastNoiseLite {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FastNoiseLite",

	delegate^self^.gdobj
}

public^let^FoldableGroup = def^{
	self^{ abstract^gdobj : godot.FoldableGroup },

	override^new = f^obj:godot.FoldableGroup {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FoldableGroup",

	delegate^self^.gdobj
}

public^let^Font = def^{
	self^{ abstract^gdobj : godot.Font },

	override^new = f^obj:godot.Font {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Font",

	delegate^self^.gdobj
}

public^let^FontFile = def^{
	self^{ abstract^gdobj : godot.FontFile },

	override^new = f^obj:godot.FontFile {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FontFile",

	delegate^self^.gdobj
}

public^let^FontVariation = def^{
	self^{ abstract^gdobj : godot.FontVariation },

	override^new = f^obj:godot.FontVariation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FontVariation",

	delegate^self^.gdobj
}

public^let^GDExtension = def^{
	self^{ abstract^gdobj : godot.GDExtension },

	override^new = f^obj:godot.GDExtension {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GDExtension",

	delegate^self^.gdobj
}

public^let^Script = def^{
	self^{ abstract^gdobj : godot.Script },

	override^new = f^obj:godot.Script {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Script",

	delegate^self^.gdobj
}

public^let^GDScript = def^{
	self^{ abstract^gdobj : godot.GDScript },

	override^new = f^obj:godot.GDScript {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GDScript",

	delegate^self^.gdobj
}

public^let^GLTFAccessor = def^{
	self^{ abstract^gdobj : godot.GLTFAccessor },

	override^new = f^obj:godot.GLTFAccessor {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFAccessor",

	delegate^self^.gdobj
}

public^let^GLTFAnimation = def^{
	self^{ abstract^gdobj : godot.GLTFAnimation },

	override^new = f^obj:godot.GLTFAnimation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFAnimation",

	delegate^self^.gdobj
}

public^let^GLTFBufferView = def^{
	self^{ abstract^gdobj : godot.GLTFBufferView },

	override^new = f^obj:godot.GLTFBufferView {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFBufferView",

	delegate^self^.gdobj
}

public^let^GLTFCamera = def^{
	self^{ abstract^gdobj : godot.GLTFCamera },

	override^new = f^obj:godot.GLTFCamera {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFCamera",

	delegate^self^.gdobj
}

public^let^GLTFDocumentExtension = def^{
	self^{ abstract^gdobj : godot.GLTFDocumentExtension },

	override^new = f^obj:godot.GLTFDocumentExtension {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFDocumentExtension",

	delegate^self^.gdobj
}

public^let^GLTFDocumentExtensionConvertImporterMesh = def^{
	self^{ abstract^gdobj : godot.GLTFDocumentExtensionConvertImporterMesh },

	override^new = f^obj:godot.GLTFDocumentExtensionConvertImporterMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFDocumentExtensionConvertImporterMesh",

	delegate^self^.gdobj
}

public^let^GLTFLight = def^{
	self^{ abstract^gdobj : godot.GLTFLight },

	override^new = f^obj:godot.GLTFLight {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFLight",

	delegate^self^.gdobj
}

public^let^GLTFMesh = def^{
	self^{ abstract^gdobj : godot.GLTFMesh },

	override^new = f^obj:godot.GLTFMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFMesh",

	delegate^self^.gdobj
}

public^let^GLTFNode = def^{
	self^{ abstract^gdobj : godot.GLTFNode },

	override^new = f^obj:godot.GLTFNode {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFNode",

	delegate^self^.gdobj
}

public^let^GLTFPhysicsBody = def^{
	self^{ abstract^gdobj : godot.GLTFPhysicsBody },

	override^new = f^obj:godot.GLTFPhysicsBody {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFPhysicsBody",

	delegate^self^.gdobj
}

public^let^GLTFPhysicsShape = def^{
	self^{ abstract^gdobj : godot.GLTFPhysicsShape },

	override^new = f^obj:godot.GLTFPhysicsShape {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFPhysicsShape",

	delegate^self^.gdobj
}

public^let^GLTFSkeleton = def^{
	self^{ abstract^gdobj : godot.GLTFSkeleton },

	override^new = f^obj:godot.GLTFSkeleton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFSkeleton",

	delegate^self^.gdobj
}

public^let^GLTFSkin = def^{
	self^{ abstract^gdobj : godot.GLTFSkin },

	override^new = f^obj:godot.GLTFSkin {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFSkin",

	delegate^self^.gdobj
}

public^let^GLTFSpecGloss = def^{
	self^{ abstract^gdobj : godot.GLTFSpecGloss },

	override^new = f^obj:godot.GLTFSpecGloss {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFSpecGloss",

	delegate^self^.gdobj
}

public^let^GLTFTexture = def^{
	self^{ abstract^gdobj : godot.GLTFTexture },

	override^new = f^obj:godot.GLTFTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFTexture",

	delegate^self^.gdobj
}

public^let^GLTFTextureSampler = def^{
	self^{ abstract^gdobj : godot.GLTFTextureSampler },

	override^new = f^obj:godot.GLTFTextureSampler {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GLTFTextureSampler",

	delegate^self^.gdobj
}

public^let^Gradient = def^{
	self^{ abstract^gdobj : godot.Gradient },

	override^new = f^obj:godot.Gradient {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Gradient",

	delegate^self^.gdobj
}

public^let^HeightMapShape3D = def^{
	self^{ abstract^gdobj : godot.HeightMapShape3D },

	override^new = f^obj:godot.HeightMapShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HeightMapShape3D",

	delegate^self^.gdobj
}

public^let^Image = def^{
	self^{ abstract^gdobj : godot.Image },

	override^new = f^obj:godot.Image {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Image",

	delegate^self^.gdobj
}

public^let^ImporterMesh = def^{
	self^{ abstract^gdobj : godot.ImporterMesh },

	override^new = f^obj:godot.ImporterMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImporterMesh",

	delegate^self^.gdobj
}

public^let^JSON = def^{
	self^{ abstract^gdobj : godot.JSON },

	override^new = f^obj:godot.JSON {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "JSON",

	delegate^self^.gdobj
}

public^let^LabelSettings = def^{
	self^{ abstract^gdobj : godot.LabelSettings },

	override^new = f^obj:godot.LabelSettings {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LabelSettings",

	delegate^self^.gdobj
}

public^let^LightmapGIData = def^{
	self^{ abstract^gdobj : godot.LightmapGIData },

	override^new = f^obj:godot.LightmapGIData {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LightmapGIData",

	delegate^self^.gdobj
}

public^let^MeshLibrary = def^{
	self^{ abstract^gdobj : godot.MeshLibrary },

	override^new = f^obj:godot.MeshLibrary {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MeshLibrary",

	delegate^self^.gdobj
}

public^let^MultiMesh = def^{
	self^{ abstract^gdobj : godot.MultiMesh },

	override^new = f^obj:godot.MultiMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MultiMesh",

	delegate^self^.gdobj
}

public^let^NavigationMesh = def^{
	self^{ abstract^gdobj : godot.NavigationMesh },

	override^new = f^obj:godot.NavigationMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationMesh",

	delegate^self^.gdobj
}

public^let^NavigationMeshSourceGeometryData2D = def^{
	self^{ abstract^gdobj : godot.NavigationMeshSourceGeometryData2D },

	override^new = f^obj:godot.NavigationMeshSourceGeometryData2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationMeshSourceGeometryData2D",

	delegate^self^.gdobj
}

public^let^NavigationMeshSourceGeometryData3D = def^{
	self^{ abstract^gdobj : godot.NavigationMeshSourceGeometryData3D },

	override^new = f^obj:godot.NavigationMeshSourceGeometryData3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationMeshSourceGeometryData3D",

	delegate^self^.gdobj
}

public^let^NavigationPolygon = def^{
	self^{ abstract^gdobj : godot.NavigationPolygon },

	override^new = f^obj:godot.NavigationPolygon {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationPolygon",

	delegate^self^.gdobj
}

public^let^OccluderPolygon2D = def^{
	self^{ abstract^gdobj : godot.OccluderPolygon2D },

	override^new = f^obj:godot.OccluderPolygon2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OccluderPolygon2D",

	delegate^self^.gdobj
}

public^let^OggPacketSequence = def^{
	self^{ abstract^gdobj : godot.OggPacketSequence },

	override^new = f^obj:godot.OggPacketSequence {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OggPacketSequence",

	delegate^self^.gdobj
}

public^let^OpenXRAction = def^{
	self^{ abstract^gdobj : godot.OpenXRAction },

	override^new = f^obj:godot.OpenXRAction {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRAction",

	delegate^self^.gdobj
}

public^let^OpenXRBindingModifier = def^{
	self^{ abstract^gdobj : godot.OpenXRBindingModifier },

	override^new = f^obj:godot.OpenXRBindingModifier {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRBindingModifier",

	delegate^self^.gdobj
}

public^let^OpenXRActionBindingModifier = def^{
	self^{ abstract^gdobj : godot.OpenXRActionBindingModifier },

	override^new = f^obj:godot.OpenXRActionBindingModifier {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRActionBindingModifier",

	delegate^self^.gdobj
}

public^let^OpenXRActionMap = def^{
	self^{ abstract^gdobj : godot.OpenXRActionMap },

	override^new = f^obj:godot.OpenXRActionMap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRActionMap",

	delegate^self^.gdobj
}

public^let^OpenXRActionSet = def^{
	self^{ abstract^gdobj : godot.OpenXRActionSet },

	override^new = f^obj:godot.OpenXRActionSet {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRActionSet",

	delegate^self^.gdobj
}

public^let^OpenXRAnalogThresholdModifier = def^{
	self^{ abstract^gdobj : godot.OpenXRAnalogThresholdModifier },

	override^new = f^obj:godot.OpenXRAnalogThresholdModifier {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRAnalogThresholdModifier",

	delegate^self^.gdobj
}

public^let^OpenXRIPBindingModifier = def^{
	self^{ abstract^gdobj : godot.OpenXRIPBindingModifier },

	override^new = f^obj:godot.OpenXRIPBindingModifier {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRIPBindingModifier",

	delegate^self^.gdobj
}

public^let^OpenXRDpadBindingModifier = def^{
	self^{ abstract^gdobj : godot.OpenXRDpadBindingModifier },

	override^new = f^obj:godot.OpenXRDpadBindingModifier {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRDpadBindingModifier",

	delegate^self^.gdobj
}

public^let^OpenXRHapticBase = def^{
	self^{ abstract^gdobj : godot.OpenXRHapticBase },

	override^new = f^obj:godot.OpenXRHapticBase {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRHapticBase",

	delegate^self^.gdobj
}

public^let^OpenXRHapticVibration = def^{
	self^{ abstract^gdobj : godot.OpenXRHapticVibration },

	override^new = f^obj:godot.OpenXRHapticVibration {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRHapticVibration",

	delegate^self^.gdobj
}

public^let^OpenXRIPBinding = def^{
	self^{ abstract^gdobj : godot.OpenXRIPBinding },

	override^new = f^obj:godot.OpenXRIPBinding {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRIPBinding",

	delegate^self^.gdobj
}

public^let^OpenXRInteractionProfile = def^{
	self^{ abstract^gdobj : godot.OpenXRInteractionProfile },

	override^new = f^obj:godot.OpenXRInteractionProfile {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRInteractionProfile",

	delegate^self^.gdobj
}

public^let^Translation = def^{
	self^{ abstract^gdobj : godot.Translation },

	override^new = f^obj:godot.Translation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Translation",

	delegate^self^.gdobj
}

public^let^OptimizedTranslation = def^{
	self^{ abstract^gdobj : godot.OptimizedTranslation },

	override^new = f^obj:godot.OptimizedTranslation {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OptimizedTranslation",

	delegate^self^.gdobj
}

public^let^PackedDataContainer = def^{
	self^{ abstract^gdobj : godot.PackedDataContainer },

	override^new = f^obj:godot.PackedDataContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PackedDataContainer",

	delegate^self^.gdobj
}

public^let^PackedScene = def^{
	self^{ abstract^gdobj : godot.PackedScene },

	override^new = f^obj:godot.PackedScene {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PackedScene",

	delegate^self^.gdobj
}

public^let^PhysicsMaterial = def^{
	self^{ abstract^gdobj : godot.PhysicsMaterial },

	override^new = f^obj:godot.PhysicsMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicsMaterial",

	delegate^self^.gdobj
}

public^let^PolygonOccluder3D = def^{
	self^{ abstract^gdobj : godot.PolygonOccluder3D },

	override^new = f^obj:godot.PolygonOccluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PolygonOccluder3D",

	delegate^self^.gdobj
}

public^let^PolygonPathFinder = def^{
	self^{ abstract^gdobj : godot.PolygonPathFinder },

	override^new = f^obj:godot.PolygonPathFinder {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PolygonPathFinder",

	delegate^self^.gdobj
}

public^let^QuadOccluder3D = def^{
	self^{ abstract^gdobj : godot.QuadOccluder3D },

	override^new = f^obj:godot.QuadOccluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "QuadOccluder3D",

	delegate^self^.gdobj
}

public^let^RDShaderFile = def^{
	self^{ abstract^gdobj : godot.RDShaderFile },

	override^new = f^obj:godot.RDShaderFile {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RDShaderFile",

	delegate^self^.gdobj
}

public^let^RDShaderSPIRV = def^{
	self^{ abstract^gdobj : godot.RDShaderSPIRV },

	override^new = f^obj:godot.RDShaderSPIRV {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RDShaderSPIRV",

	delegate^self^.gdobj
}

public^let^RectangleShape2D = def^{
	self^{ abstract^gdobj : godot.RectangleShape2D },

	override^new = f^obj:godot.RectangleShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RectangleShape2D",

	delegate^self^.gdobj
}

public^let^RichTextEffect = def^{
	self^{ abstract^gdobj : godot.RichTextEffect },

	override^new = f^obj:godot.RichTextEffect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RichTextEffect",

	delegate^self^.gdobj
}

public^let^SceneReplicationConfig = def^{
	self^{ abstract^gdobj : godot.SceneReplicationConfig },

	override^new = f^obj:godot.SceneReplicationConfig {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SceneReplicationConfig",

	delegate^self^.gdobj
}

public^let^ScriptExtension = def^{
	self^{ abstract^gdobj : godot.ScriptExtension },

	override^new = f^obj:godot.ScriptExtension {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ScriptExtension",

	delegate^self^.gdobj
}

public^let^SegmentShape2D = def^{
	self^{ abstract^gdobj : godot.SegmentShape2D },

	override^new = f^obj:godot.SegmentShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SegmentShape2D",

	delegate^self^.gdobj
}

public^let^SeparationRayShape2D = def^{
	self^{ abstract^gdobj : godot.SeparationRayShape2D },

	override^new = f^obj:godot.SeparationRayShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SeparationRayShape2D",

	delegate^self^.gdobj
}

public^let^SeparationRayShape3D = def^{
	self^{ abstract^gdobj : godot.SeparationRayShape3D },

	override^new = f^obj:godot.SeparationRayShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SeparationRayShape3D",

	delegate^self^.gdobj
}

public^let^Shader = def^{
	self^{ abstract^gdobj : godot.Shader },

	override^new = f^obj:godot.Shader {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Shader",

	delegate^self^.gdobj
}

public^let^ShaderInclude = def^{
	self^{ abstract^gdobj : godot.ShaderInclude },

	override^new = f^obj:godot.ShaderInclude {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ShaderInclude",

	delegate^self^.gdobj
}

public^let^Shortcut = def^{
	self^{ abstract^gdobj : godot.Shortcut },

	override^new = f^obj:godot.Shortcut {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Shortcut",

	delegate^self^.gdobj
}

public^let^SkeletonModification2D = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2D },

	override^new = f^obj:godot.SkeletonModification2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2D",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DCCDIK = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DCCDIK },

	override^new = f^obj:godot.SkeletonModification2DCCDIK {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DCCDIK",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DFABRIK = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DFABRIK },

	override^new = f^obj:godot.SkeletonModification2DFABRIK {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DFABRIK",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DJiggle = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DJiggle },

	override^new = f^obj:godot.SkeletonModification2DJiggle {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DJiggle",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DLookAt = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DLookAt },

	override^new = f^obj:godot.SkeletonModification2DLookAt {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DLookAt",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DPhysicalBones = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DPhysicalBones },

	override^new = f^obj:godot.SkeletonModification2DPhysicalBones {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DPhysicalBones",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DStackHolder = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DStackHolder },

	override^new = f^obj:godot.SkeletonModification2DStackHolder {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DStackHolder",

	delegate^self^.gdobj
}

public^let^SkeletonModification2DTwoBoneIK = def^{
	self^{ abstract^gdobj : godot.SkeletonModification2DTwoBoneIK },

	override^new = f^obj:godot.SkeletonModification2DTwoBoneIK {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModification2DTwoBoneIK",

	delegate^self^.gdobj
}

public^let^SkeletonModificationStack2D = def^{
	self^{ abstract^gdobj : godot.SkeletonModificationStack2D },

	override^new = f^obj:godot.SkeletonModificationStack2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModificationStack2D",

	delegate^self^.gdobj
}

public^let^SkeletonProfile = def^{
	self^{ abstract^gdobj : godot.SkeletonProfile },

	override^new = f^obj:godot.SkeletonProfile {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonProfile",

	delegate^self^.gdobj
}

public^let^SkeletonProfileHumanoid = def^{
	self^{ abstract^gdobj : godot.SkeletonProfileHumanoid },

	override^new = f^obj:godot.SkeletonProfileHumanoid {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonProfileHumanoid",

	delegate^self^.gdobj
}

public^let^Skin = def^{
	self^{ abstract^gdobj : godot.Skin },

	override^new = f^obj:godot.Skin {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Skin",

	delegate^self^.gdobj
}

public^let^Sky = def^{
	self^{ abstract^gdobj : godot.Sky },

	override^new = f^obj:godot.Sky {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Sky",

	delegate^self^.gdobj
}

public^let^SphereOccluder3D = def^{
	self^{ abstract^gdobj : godot.SphereOccluder3D },

	override^new = f^obj:godot.SphereOccluder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SphereOccluder3D",

	delegate^self^.gdobj
}

public^let^SphereShape3D = def^{
	self^{ abstract^gdobj : godot.SphereShape3D },

	override^new = f^obj:godot.SphereShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SphereShape3D",

	delegate^self^.gdobj
}

public^let^SpriteFrames = def^{
	self^{ abstract^gdobj : godot.SpriteFrames },

	override^new = f^obj:godot.SpriteFrames {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpriteFrames",

	delegate^self^.gdobj
}

public^let^StyleBox = def^{
	self^{ abstract^gdobj : godot.StyleBox },

	override^new = f^obj:godot.StyleBox {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StyleBox",

	delegate^self^.gdobj
}

public^let^StyleBoxEmpty = def^{
	self^{ abstract^gdobj : godot.StyleBoxEmpty },

	override^new = f^obj:godot.StyleBoxEmpty {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StyleBoxEmpty",

	delegate^self^.gdobj
}

public^let^StyleBoxFlat = def^{
	self^{ abstract^gdobj : godot.StyleBoxFlat },

	override^new = f^obj:godot.StyleBoxFlat {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StyleBoxFlat",

	delegate^self^.gdobj
}

public^let^StyleBoxLine = def^{
	self^{ abstract^gdobj : godot.StyleBoxLine },

	override^new = f^obj:godot.StyleBoxLine {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StyleBoxLine",

	delegate^self^.gdobj
}

public^let^StyleBoxTexture = def^{
	self^{ abstract^gdobj : godot.StyleBoxTexture },

	override^new = f^obj:godot.StyleBoxTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StyleBoxTexture",

	delegate^self^.gdobj
}

public^let^SystemFont = def^{
	self^{ abstract^gdobj : godot.SystemFont },

	override^new = f^obj:godot.SystemFont {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SystemFont",

	delegate^self^.gdobj
}

public^let^Theme = def^{
	self^{ abstract^gdobj : godot.Theme },

	override^new = f^obj:godot.Theme {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Theme",

	delegate^self^.gdobj
}

public^let^TileMapPattern = def^{
	self^{ abstract^gdobj : godot.TileMapPattern },

	override^new = f^obj:godot.TileMapPattern {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileMapPattern",

	delegate^self^.gdobj
}

public^let^TileSet = def^{
	self^{ abstract^gdobj : godot.TileSet },

	override^new = f^obj:godot.TileSet {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileSet",

	delegate^self^.gdobj
}

public^let^TileSetSource = def^{
	self^{ abstract^gdobj : godot.TileSetSource },

	override^new = f^obj:godot.TileSetSource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileSetSource",

	delegate^self^.gdobj
}

public^let^TileSetAtlasSource = def^{
	self^{ abstract^gdobj : godot.TileSetAtlasSource },

	override^new = f^obj:godot.TileSetAtlasSource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileSetAtlasSource",

	delegate^self^.gdobj
}

public^let^TileSetScenesCollectionSource = def^{
	self^{ abstract^gdobj : godot.TileSetScenesCollectionSource },

	override^new = f^obj:godot.TileSetScenesCollectionSource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileSetScenesCollectionSource",

	delegate^self^.gdobj
}

public^let^VideoStream = def^{
	self^{ abstract^gdobj : godot.VideoStream },

	override^new = f^obj:godot.VideoStream {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VideoStream",

	delegate^self^.gdobj
}

public^let^VideoStreamPlayback = def^{
	self^{ abstract^gdobj : godot.VideoStreamPlayback },

	override^new = f^obj:godot.VideoStreamPlayback {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VideoStreamPlayback",

	delegate^self^.gdobj
}

public^let^VideoStreamTheora = def^{
	self^{ abstract^gdobj : godot.VideoStreamTheora },

	override^new = f^obj:godot.VideoStreamTheora {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VideoStreamTheora",

	delegate^self^.gdobj
}

public^let^VisualShader = def^{
	self^{ abstract^gdobj : godot.VisualShader },

	override^new = f^obj:godot.VisualShader {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShader",

	delegate^self^.gdobj
}

public^let^VoxelGIData = def^{
	self^{ abstract^gdobj : godot.VoxelGIData },

	override^new = f^obj:godot.VoxelGIData {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VoxelGIData",

	delegate^self^.gdobj
}

public^let^World2D = def^{
	self^{ abstract^gdobj : godot.World2D },

	override^new = f^obj:godot.World2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "World2D",

	delegate^self^.gdobj
}

public^let^World3D = def^{
	self^{ abstract^gdobj : godot.World3D },

	override^new = f^obj:godot.World3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "World3D",

	delegate^self^.gdobj
}

public^let^WorldBoundaryShape2D = def^{
	self^{ abstract^gdobj : godot.WorldBoundaryShape2D },

	override^new = f^obj:godot.WorldBoundaryShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "WorldBoundaryShape2D",

	delegate^self^.gdobj
}

public^let^WorldBoundaryShape3D = def^{
	self^{ abstract^gdobj : godot.WorldBoundaryShape3D },

	override^new = f^obj:godot.WorldBoundaryShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "WorldBoundaryShape3D",

	delegate^self^.gdobj
}

public^let^X509Certificate = def^{
	self^{ abstract^gdobj : godot.X509Certificate },

	override^new = f^obj:godot.X509Certificate {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "X509Certificate",

	delegate^self^.gdobj
}
