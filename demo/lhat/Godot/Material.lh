# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Material. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Material

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^Material = def^{
	self^{ abstract^gdobj : godot.Material },

	override^new = f^obj:godot.Material {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Material",

	delegate^self^.gdobj
}

public^let^CanvasItemMaterial = def^{
	self^{ abstract^gdobj : godot.CanvasItemMaterial },

	override^new = f^obj:godot.CanvasItemMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasItemMaterial",

	delegate^self^.gdobj
}

public^let^FogMaterial = def^{
	self^{ abstract^gdobj : godot.FogMaterial },

	override^new = f^obj:godot.FogMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FogMaterial",

	delegate^self^.gdobj
}

public^let^BaseMaterial3D = def^{
	self^{ abstract^gdobj : godot.BaseMaterial3D },

	override^new = f^obj:godot.BaseMaterial3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BaseMaterial3D",

	delegate^self^.gdobj
}

public^let^ORMMaterial3D = def^{
	self^{ abstract^gdobj : godot.ORMMaterial3D },

	override^new = f^obj:godot.ORMMaterial3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ORMMaterial3D",

	delegate^self^.gdobj
}

public^let^PanoramaSkyMaterial = def^{
	self^{ abstract^gdobj : godot.PanoramaSkyMaterial },

	override^new = f^obj:godot.PanoramaSkyMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PanoramaSkyMaterial",

	delegate^self^.gdobj
}

public^let^ParticleProcessMaterial = def^{
	self^{ abstract^gdobj : godot.ParticleProcessMaterial },

	override^new = f^obj:godot.ParticleProcessMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ParticleProcessMaterial",

	delegate^self^.gdobj
}

public^let^PhysicalSkyMaterial = def^{
	self^{ abstract^gdobj : godot.PhysicalSkyMaterial },

	override^new = f^obj:godot.PhysicalSkyMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicalSkyMaterial",

	delegate^self^.gdobj
}

public^let^PlaceholderMaterial = def^{
	self^{ abstract^gdobj : godot.PlaceholderMaterial },

	override^new = f^obj:godot.PlaceholderMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderMaterial",

	delegate^self^.gdobj
}

public^let^ProceduralSkyMaterial = def^{
	self^{ abstract^gdobj : godot.ProceduralSkyMaterial },

	override^new = f^obj:godot.ProceduralSkyMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ProceduralSkyMaterial",

	delegate^self^.gdobj
}

public^let^ShaderMaterial = def^{
	self^{ abstract^gdobj : godot.ShaderMaterial },

	override^new = f^obj:godot.ShaderMaterial {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ShaderMaterial",

	delegate^self^.gdobj
}

public^let^StandardMaterial3D = def^{
	self^{ abstract^gdobj : godot.StandardMaterial3D },

	override^new = f^obj:godot.StandardMaterial3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StandardMaterial3D",

	delegate^self^.gdobj
}
