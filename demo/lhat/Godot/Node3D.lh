# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Node3D. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Node3D

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^Node = def^{
	self^{ abstract^gdobj : godot.Node },

	override^new = f^obj:godot.Node {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node",

	delegate^self^.gdobj
}

public^let^Node3D = def^{
	self^{ abstract^gdobj : godot.Node3D },

	override^new = f^obj:godot.Node3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node3D",

	delegate^self^.gdobj
}

public^let^SkeletonModifier3D = def^{
	self^{ abstract^gdobj : godot.SkeletonModifier3D },

	override^new = f^obj:godot.SkeletonModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonModifier3D",

	delegate^self^.gdobj
}

public^let^BoneConstraint3D = def^{
	self^{ abstract^gdobj : godot.BoneConstraint3D },

	override^new = f^obj:godot.BoneConstraint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoneConstraint3D",

	delegate^self^.gdobj
}

public^let^AimModifier3D = def^{
	self^{ abstract^gdobj : godot.AimModifier3D },

	override^new = f^obj:godot.AimModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AimModifier3D",

	delegate^self^.gdobj
}

public^let^CollisionObject3D = def^{
	self^{ abstract^gdobj : godot.CollisionObject3D },

	override^new = f^obj:godot.CollisionObject3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionObject3D",

	delegate^self^.gdobj
}

public^let^PhysicsBody3D = def^{
	self^{ abstract^gdobj : godot.PhysicsBody3D },

	override^new = f^obj:godot.PhysicsBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicsBody3D",

	delegate^self^.gdobj
}

public^let^StaticBody3D = def^{
	self^{ abstract^gdobj : godot.StaticBody3D },

	override^new = f^obj:godot.StaticBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StaticBody3D",

	delegate^self^.gdobj
}

public^let^AnimatableBody3D = def^{
	self^{ abstract^gdobj : godot.AnimatableBody3D },

	override^new = f^obj:godot.AnimatableBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimatableBody3D",

	delegate^self^.gdobj
}

public^let^VisualInstance3D = def^{
	self^{ abstract^gdobj : godot.VisualInstance3D },

	override^new = f^obj:godot.VisualInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualInstance3D",

	delegate^self^.gdobj
}

public^let^GeometryInstance3D = def^{
	self^{ abstract^gdobj : godot.GeometryInstance3D },

	override^new = f^obj:godot.GeometryInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GeometryInstance3D",

	delegate^self^.gdobj
}

public^let^SpriteBase3D = def^{
	self^{ abstract^gdobj : godot.SpriteBase3D },

	override^new = f^obj:godot.SpriteBase3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpriteBase3D",

	delegate^self^.gdobj
}

public^let^AnimatedSprite3D = def^{
	self^{ abstract^gdobj : godot.AnimatedSprite3D },

	override^new = f^obj:godot.AnimatedSprite3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimatedSprite3D",

	delegate^self^.gdobj
}

public^let^Area3D = def^{
	self^{ abstract^gdobj : godot.Area3D },

	override^new = f^obj:godot.Area3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Area3D",

	delegate^self^.gdobj
}

public^let^AudioListener3D = def^{
	self^{ abstract^gdobj : godot.AudioListener3D },

	override^new = f^obj:godot.AudioListener3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioListener3D",

	delegate^self^.gdobj
}

public^let^AudioStreamPlayer3D = def^{
	self^{ abstract^gdobj : godot.AudioStreamPlayer3D },

	override^new = f^obj:godot.AudioStreamPlayer3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamPlayer3D",

	delegate^self^.gdobj
}

public^let^BoneAttachment3D = def^{
	self^{ abstract^gdobj : godot.BoneAttachment3D },

	override^new = f^obj:godot.BoneAttachment3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoneAttachment3D",

	delegate^self^.gdobj
}

public^let^CPUParticles3D = def^{
	self^{ abstract^gdobj : godot.CPUParticles3D },

	override^new = f^obj:godot.CPUParticles3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CPUParticles3D",

	delegate^self^.gdobj
}

public^let^CSGShape3D = def^{
	self^{ abstract^gdobj : godot.CSGShape3D },

	override^new = f^obj:godot.CSGShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGShape3D",

	delegate^self^.gdobj
}

public^let^CSGPrimitive3D = def^{
	self^{ abstract^gdobj : godot.CSGPrimitive3D },

	override^new = f^obj:godot.CSGPrimitive3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGPrimitive3D",

	delegate^self^.gdobj
}

public^let^CSGBox3D = def^{
	self^{ abstract^gdobj : godot.CSGBox3D },

	override^new = f^obj:godot.CSGBox3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGBox3D",

	delegate^self^.gdobj
}

public^let^CSGCombiner3D = def^{
	self^{ abstract^gdobj : godot.CSGCombiner3D },

	override^new = f^obj:godot.CSGCombiner3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGCombiner3D",

	delegate^self^.gdobj
}

public^let^CSGCylinder3D = def^{
	self^{ abstract^gdobj : godot.CSGCylinder3D },

	override^new = f^obj:godot.CSGCylinder3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGCylinder3D",

	delegate^self^.gdobj
}

public^let^CSGMesh3D = def^{
	self^{ abstract^gdobj : godot.CSGMesh3D },

	override^new = f^obj:godot.CSGMesh3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGMesh3D",

	delegate^self^.gdobj
}

public^let^CSGPolygon3D = def^{
	self^{ abstract^gdobj : godot.CSGPolygon3D },

	override^new = f^obj:godot.CSGPolygon3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGPolygon3D",

	delegate^self^.gdobj
}

public^let^CSGSphere3D = def^{
	self^{ abstract^gdobj : godot.CSGSphere3D },

	override^new = f^obj:godot.CSGSphere3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGSphere3D",

	delegate^self^.gdobj
}

public^let^CSGTorus3D = def^{
	self^{ abstract^gdobj : godot.CSGTorus3D },

	override^new = f^obj:godot.CSGTorus3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CSGTorus3D",

	delegate^self^.gdobj
}

public^let^Camera3D = def^{
	self^{ abstract^gdobj : godot.Camera3D },

	override^new = f^obj:godot.Camera3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Camera3D",

	delegate^self^.gdobj
}

public^let^CharacterBody3D = def^{
	self^{ abstract^gdobj : godot.CharacterBody3D },

	override^new = f^obj:godot.CharacterBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CharacterBody3D",

	delegate^self^.gdobj
}

public^let^CollisionPolygon3D = def^{
	self^{ abstract^gdobj : godot.CollisionPolygon3D },

	override^new = f^obj:godot.CollisionPolygon3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionPolygon3D",

	delegate^self^.gdobj
}

public^let^CollisionShape3D = def^{
	self^{ abstract^gdobj : godot.CollisionShape3D },

	override^new = f^obj:godot.CollisionShape3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionShape3D",

	delegate^self^.gdobj
}

public^let^Joint3D = def^{
	self^{ abstract^gdobj : godot.Joint3D },

	override^new = f^obj:godot.Joint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Joint3D",

	delegate^self^.gdobj
}

public^let^ConeTwistJoint3D = def^{
	self^{ abstract^gdobj : godot.ConeTwistJoint3D },

	override^new = f^obj:godot.ConeTwistJoint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConeTwistJoint3D",

	delegate^self^.gdobj
}

public^let^ConvertTransformModifier3D = def^{
	self^{ abstract^gdobj : godot.ConvertTransformModifier3D },

	override^new = f^obj:godot.ConvertTransformModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConvertTransformModifier3D",

	delegate^self^.gdobj
}

public^let^CopyTransformModifier3D = def^{
	self^{ abstract^gdobj : godot.CopyTransformModifier3D },

	override^new = f^obj:godot.CopyTransformModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CopyTransformModifier3D",

	delegate^self^.gdobj
}

public^let^Decal = def^{
	self^{ abstract^gdobj : godot.Decal },

	override^new = f^obj:godot.Decal {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Decal",

	delegate^self^.gdobj
}

public^let^Light3D = def^{
	self^{ abstract^gdobj : godot.Light3D },

	override^new = f^obj:godot.Light3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Light3D",

	delegate^self^.gdobj
}

public^let^DirectionalLight3D = def^{
	self^{ abstract^gdobj : godot.DirectionalLight3D },

	override^new = f^obj:godot.DirectionalLight3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "DirectionalLight3D",

	delegate^self^.gdobj
}

public^let^FogVolume = def^{
	self^{ abstract^gdobj : godot.FogVolume },

	override^new = f^obj:godot.FogVolume {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FogVolume",

	delegate^self^.gdobj
}

public^let^GPUParticles3D = def^{
	self^{ abstract^gdobj : godot.GPUParticles3D },

	override^new = f^obj:godot.GPUParticles3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticles3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesAttractor3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesAttractor3D },

	override^new = f^obj:godot.GPUParticlesAttractor3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesAttractor3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesAttractorBox3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesAttractorBox3D },

	override^new = f^obj:godot.GPUParticlesAttractorBox3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesAttractorBox3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesAttractorSphere3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesAttractorSphere3D },

	override^new = f^obj:godot.GPUParticlesAttractorSphere3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesAttractorSphere3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesAttractorVectorField3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesAttractorVectorField3D },

	override^new = f^obj:godot.GPUParticlesAttractorVectorField3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesAttractorVectorField3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesCollision3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesCollision3D },

	override^new = f^obj:godot.GPUParticlesCollision3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesCollision3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesCollisionBox3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesCollisionBox3D },

	override^new = f^obj:godot.GPUParticlesCollisionBox3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesCollisionBox3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesCollisionHeightField3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesCollisionHeightField3D },

	override^new = f^obj:godot.GPUParticlesCollisionHeightField3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesCollisionHeightField3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesCollisionSDF3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesCollisionSDF3D },

	override^new = f^obj:godot.GPUParticlesCollisionSDF3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesCollisionSDF3D",

	delegate^self^.gdobj
}

public^let^GPUParticlesCollisionSphere3D = def^{
	self^{ abstract^gdobj : godot.GPUParticlesCollisionSphere3D },

	override^new = f^obj:godot.GPUParticlesCollisionSphere3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticlesCollisionSphere3D",

	delegate^self^.gdobj
}

public^let^Generic6DOFJoint3D = def^{
	self^{ abstract^gdobj : godot.Generic6DOFJoint3D },

	override^new = f^obj:godot.Generic6DOFJoint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Generic6DOFJoint3D",

	delegate^self^.gdobj
}

public^let^GridMap = def^{
	self^{ abstract^gdobj : godot.GridMap },

	override^new = f^obj:godot.GridMap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GridMap",

	delegate^self^.gdobj
}

public^let^HingeJoint3D = def^{
	self^{ abstract^gdobj : godot.HingeJoint3D },

	override^new = f^obj:godot.HingeJoint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HingeJoint3D",

	delegate^self^.gdobj
}

public^let^ImporterMeshInstance3D = def^{
	self^{ abstract^gdobj : godot.ImporterMeshInstance3D },

	override^new = f^obj:godot.ImporterMeshInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImporterMeshInstance3D",

	delegate^self^.gdobj
}

public^let^Label3D = def^{
	self^{ abstract^gdobj : godot.Label3D },

	override^new = f^obj:godot.Label3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Label3D",

	delegate^self^.gdobj
}

public^let^LightmapGI = def^{
	self^{ abstract^gdobj : godot.LightmapGI },

	override^new = f^obj:godot.LightmapGI {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LightmapGI",

	delegate^self^.gdobj
}

public^let^LightmapProbe = def^{
	self^{ abstract^gdobj : godot.LightmapProbe },

	override^new = f^obj:godot.LightmapProbe {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LightmapProbe",

	delegate^self^.gdobj
}

public^let^LookAtModifier3D = def^{
	self^{ abstract^gdobj : godot.LookAtModifier3D },

	override^new = f^obj:godot.LookAtModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LookAtModifier3D",

	delegate^self^.gdobj
}

public^let^Marker3D = def^{
	self^{ abstract^gdobj : godot.Marker3D },

	override^new = f^obj:godot.Marker3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Marker3D",

	delegate^self^.gdobj
}

public^let^MeshInstance3D = def^{
	self^{ abstract^gdobj : godot.MeshInstance3D },

	override^new = f^obj:godot.MeshInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MeshInstance3D",

	delegate^self^.gdobj
}

public^let^ModifierBoneTarget3D = def^{
	self^{ abstract^gdobj : godot.ModifierBoneTarget3D },

	override^new = f^obj:godot.ModifierBoneTarget3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ModifierBoneTarget3D",

	delegate^self^.gdobj
}

public^let^MultiMeshInstance3D = def^{
	self^{ abstract^gdobj : godot.MultiMeshInstance3D },

	override^new = f^obj:godot.MultiMeshInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MultiMeshInstance3D",

	delegate^self^.gdobj
}

public^let^NavigationLink3D = def^{
	self^{ abstract^gdobj : godot.NavigationLink3D },

	override^new = f^obj:godot.NavigationLink3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationLink3D",

	delegate^self^.gdobj
}

public^let^NavigationObstacle3D = def^{
	self^{ abstract^gdobj : godot.NavigationObstacle3D },

	override^new = f^obj:godot.NavigationObstacle3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationObstacle3D",

	delegate^self^.gdobj
}

public^let^NavigationRegion3D = def^{
	self^{ abstract^gdobj : godot.NavigationRegion3D },

	override^new = f^obj:godot.NavigationRegion3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationRegion3D",

	delegate^self^.gdobj
}

public^let^OccluderInstance3D = def^{
	self^{ abstract^gdobj : godot.OccluderInstance3D },

	override^new = f^obj:godot.OccluderInstance3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OccluderInstance3D",

	delegate^self^.gdobj
}

public^let^OmniLight3D = def^{
	self^{ abstract^gdobj : godot.OmniLight3D },

	override^new = f^obj:godot.OmniLight3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OmniLight3D",

	delegate^self^.gdobj
}

public^let^OpenXRCompositionLayer = def^{
	self^{ abstract^gdobj : godot.OpenXRCompositionLayer },

	override^new = f^obj:godot.OpenXRCompositionLayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRCompositionLayer",

	delegate^self^.gdobj
}

public^let^OpenXRCompositionLayerCylinder = def^{
	self^{ abstract^gdobj : godot.OpenXRCompositionLayerCylinder },

	override^new = f^obj:godot.OpenXRCompositionLayerCylinder {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRCompositionLayerCylinder",

	delegate^self^.gdobj
}

public^let^OpenXRCompositionLayerEquirect = def^{
	self^{ abstract^gdobj : godot.OpenXRCompositionLayerEquirect },

	override^new = f^obj:godot.OpenXRCompositionLayerEquirect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRCompositionLayerEquirect",

	delegate^self^.gdobj
}

public^let^OpenXRCompositionLayerQuad = def^{
	self^{ abstract^gdobj : godot.OpenXRCompositionLayerQuad },

	override^new = f^obj:godot.OpenXRCompositionLayerQuad {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRCompositionLayerQuad",

	delegate^self^.gdobj
}

public^let^OpenXRHand = def^{
	self^{ abstract^gdobj : godot.OpenXRHand },

	override^new = f^obj:godot.OpenXRHand {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRHand",

	delegate^self^.gdobj
}

public^let^OpenXRRenderModel = def^{
	self^{ abstract^gdobj : godot.OpenXRRenderModel },

	override^new = f^obj:godot.OpenXRRenderModel {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRRenderModel",

	delegate^self^.gdobj
}

public^let^OpenXRRenderModelManager = def^{
	self^{ abstract^gdobj : godot.OpenXRRenderModelManager },

	override^new = f^obj:godot.OpenXRRenderModelManager {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRRenderModelManager",

	delegate^self^.gdobj
}

public^let^OpenXRVisibilityMask = def^{
	self^{ abstract^gdobj : godot.OpenXRVisibilityMask },

	override^new = f^obj:godot.OpenXRVisibilityMask {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OpenXRVisibilityMask",

	delegate^self^.gdobj
}

public^let^Path3D = def^{
	self^{ abstract^gdobj : godot.Path3D },

	override^new = f^obj:godot.Path3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Path3D",

	delegate^self^.gdobj
}

public^let^PathFollow3D = def^{
	self^{ abstract^gdobj : godot.PathFollow3D },

	override^new = f^obj:godot.PathFollow3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PathFollow3D",

	delegate^self^.gdobj
}

public^let^PhysicalBone3D = def^{
	self^{ abstract^gdobj : godot.PhysicalBone3D },

	override^new = f^obj:godot.PhysicalBone3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicalBone3D",

	delegate^self^.gdobj
}

public^let^PhysicalBoneSimulator3D = def^{
	self^{ abstract^gdobj : godot.PhysicalBoneSimulator3D },

	override^new = f^obj:godot.PhysicalBoneSimulator3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicalBoneSimulator3D",

	delegate^self^.gdobj
}

public^let^PinJoint3D = def^{
	self^{ abstract^gdobj : godot.PinJoint3D },

	override^new = f^obj:godot.PinJoint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PinJoint3D",

	delegate^self^.gdobj
}

public^let^RayCast3D = def^{
	self^{ abstract^gdobj : godot.RayCast3D },

	override^new = f^obj:godot.RayCast3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RayCast3D",

	delegate^self^.gdobj
}

public^let^ReflectionProbe = def^{
	self^{ abstract^gdobj : godot.ReflectionProbe },

	override^new = f^obj:godot.ReflectionProbe {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ReflectionProbe",

	delegate^self^.gdobj
}

public^let^RemoteTransform3D = def^{
	self^{ abstract^gdobj : godot.RemoteTransform3D },

	override^new = f^obj:godot.RemoteTransform3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RemoteTransform3D",

	delegate^self^.gdobj
}

public^let^RetargetModifier3D = def^{
	self^{ abstract^gdobj : godot.RetargetModifier3D },

	override^new = f^obj:godot.RetargetModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RetargetModifier3D",

	delegate^self^.gdobj
}

public^let^RigidBody3D = def^{
	self^{ abstract^gdobj : godot.RigidBody3D },

	override^new = f^obj:godot.RigidBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RigidBody3D",

	delegate^self^.gdobj
}

public^let^RootMotionView = def^{
	self^{ abstract^gdobj : godot.RootMotionView },

	override^new = f^obj:godot.RootMotionView {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RootMotionView",

	delegate^self^.gdobj
}

public^let^ShapeCast3D = def^{
	self^{ abstract^gdobj : godot.ShapeCast3D },

	override^new = f^obj:godot.ShapeCast3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ShapeCast3D",

	delegate^self^.gdobj
}

public^let^Skeleton3D = def^{
	self^{ abstract^gdobj : godot.Skeleton3D },

	override^new = f^obj:godot.Skeleton3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Skeleton3D",

	delegate^self^.gdobj
}

public^let^SkeletonIK3D = def^{
	self^{ abstract^gdobj : godot.SkeletonIK3D },

	override^new = f^obj:godot.SkeletonIK3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SkeletonIK3D",

	delegate^self^.gdobj
}

public^let^SliderJoint3D = def^{
	self^{ abstract^gdobj : godot.SliderJoint3D },

	override^new = f^obj:godot.SliderJoint3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SliderJoint3D",

	delegate^self^.gdobj
}

public^let^SoftBody3D = def^{
	self^{ abstract^gdobj : godot.SoftBody3D },

	override^new = f^obj:godot.SoftBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SoftBody3D",

	delegate^self^.gdobj
}

public^let^SpotLight3D = def^{
	self^{ abstract^gdobj : godot.SpotLight3D },

	override^new = f^obj:godot.SpotLight3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpotLight3D",

	delegate^self^.gdobj
}

public^let^SpringArm3D = def^{
	self^{ abstract^gdobj : godot.SpringArm3D },

	override^new = f^obj:godot.SpringArm3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringArm3D",

	delegate^self^.gdobj
}

public^let^SpringBoneCollision3D = def^{
	self^{ abstract^gdobj : godot.SpringBoneCollision3D },

	override^new = f^obj:godot.SpringBoneCollision3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringBoneCollision3D",

	delegate^self^.gdobj
}

public^let^SpringBoneCollisionCapsule3D = def^{
	self^{ abstract^gdobj : godot.SpringBoneCollisionCapsule3D },

	override^new = f^obj:godot.SpringBoneCollisionCapsule3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringBoneCollisionCapsule3D",

	delegate^self^.gdobj
}

public^let^SpringBoneCollisionPlane3D = def^{
	self^{ abstract^gdobj : godot.SpringBoneCollisionPlane3D },

	override^new = f^obj:godot.SpringBoneCollisionPlane3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringBoneCollisionPlane3D",

	delegate^self^.gdobj
}

public^let^SpringBoneCollisionSphere3D = def^{
	self^{ abstract^gdobj : godot.SpringBoneCollisionSphere3D },

	override^new = f^obj:godot.SpringBoneCollisionSphere3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringBoneCollisionSphere3D",

	delegate^self^.gdobj
}

public^let^SpringBoneSimulator3D = def^{
	self^{ abstract^gdobj : godot.SpringBoneSimulator3D },

	override^new = f^obj:godot.SpringBoneSimulator3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpringBoneSimulator3D",

	delegate^self^.gdobj
}

public^let^Sprite3D = def^{
	self^{ abstract^gdobj : godot.Sprite3D },

	override^new = f^obj:godot.Sprite3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Sprite3D",

	delegate^self^.gdobj
}

public^let^VehicleBody3D = def^{
	self^{ abstract^gdobj : godot.VehicleBody3D },

	override^new = f^obj:godot.VehicleBody3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VehicleBody3D",

	delegate^self^.gdobj
}

public^let^VehicleWheel3D = def^{
	self^{ abstract^gdobj : godot.VehicleWheel3D },

	override^new = f^obj:godot.VehicleWheel3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VehicleWheel3D",

	delegate^self^.gdobj
}

public^let^VisibleOnScreenNotifier3D = def^{
	self^{ abstract^gdobj : godot.VisibleOnScreenNotifier3D },

	override^new = f^obj:godot.VisibleOnScreenNotifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisibleOnScreenNotifier3D",

	delegate^self^.gdobj
}

public^let^VisibleOnScreenEnabler3D = def^{
	self^{ abstract^gdobj : godot.VisibleOnScreenEnabler3D },

	override^new = f^obj:godot.VisibleOnScreenEnabler3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisibleOnScreenEnabler3D",

	delegate^self^.gdobj
}

public^let^VoxelGI = def^{
	self^{ abstract^gdobj : godot.VoxelGI },

	override^new = f^obj:godot.VoxelGI {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VoxelGI",

	delegate^self^.gdobj
}

public^let^XRNode3D = def^{
	self^{ abstract^gdobj : godot.XRNode3D },

	override^new = f^obj:godot.XRNode3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRNode3D",

	delegate^self^.gdobj
}

public^let^XRAnchor3D = def^{
	self^{ abstract^gdobj : godot.XRAnchor3D },

	override^new = f^obj:godot.XRAnchor3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRAnchor3D",

	delegate^self^.gdobj
}

public^let^XRBodyModifier3D = def^{
	self^{ abstract^gdobj : godot.XRBodyModifier3D },

	override^new = f^obj:godot.XRBodyModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRBodyModifier3D",

	delegate^self^.gdobj
}

public^let^XRCamera3D = def^{
	self^{ abstract^gdobj : godot.XRCamera3D },

	override^new = f^obj:godot.XRCamera3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRCamera3D",

	delegate^self^.gdobj
}

public^let^XRController3D = def^{
	self^{ abstract^gdobj : godot.XRController3D },

	override^new = f^obj:godot.XRController3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRController3D",

	delegate^self^.gdobj
}

public^let^XRFaceModifier3D = def^{
	self^{ abstract^gdobj : godot.XRFaceModifier3D },

	override^new = f^obj:godot.XRFaceModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRFaceModifier3D",

	delegate^self^.gdobj
}

public^let^XRHandModifier3D = def^{
	self^{ abstract^gdobj : godot.XRHandModifier3D },

	override^new = f^obj:godot.XRHandModifier3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XRHandModifier3D",

	delegate^self^.gdobj
}

public^let^XROrigin3D = def^{
	self^{ abstract^gdobj : godot.XROrigin3D },

	override^new = f^obj:godot.XROrigin3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "XROrigin3D",

	delegate^self^.gdobj
}
