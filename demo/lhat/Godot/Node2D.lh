# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Node2D. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Node2D

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^Node = def^{
	self^{ abstract^gdobj : godot.Node },

	override^new = f^obj:godot.Node {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node",

	delegate^self^.gdobj
}

public^let^CanvasItem = def^{
	self^{ abstract^gdobj : godot.CanvasItem },

	override^new = f^obj:godot.CanvasItem {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasItem",

	delegate^self^.gdobj
}

public^let^Node2D = def^{
	self^{ abstract^gdobj : godot.Node2D },

	override^new = f^obj:godot.Node2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node2D",

	delegate^self^.gdobj
}

public^let^CollisionObject2D = def^{
	self^{ abstract^gdobj : godot.CollisionObject2D },

	override^new = f^obj:godot.CollisionObject2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionObject2D",

	delegate^self^.gdobj
}

public^let^PhysicsBody2D = def^{
	self^{ abstract^gdobj : godot.PhysicsBody2D },

	override^new = f^obj:godot.PhysicsBody2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicsBody2D",

	delegate^self^.gdobj
}

public^let^StaticBody2D = def^{
	self^{ abstract^gdobj : godot.StaticBody2D },

	override^new = f^obj:godot.StaticBody2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StaticBody2D",

	delegate^self^.gdobj
}

public^let^AnimatableBody2D = def^{
	self^{ abstract^gdobj : godot.AnimatableBody2D },

	override^new = f^obj:godot.AnimatableBody2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimatableBody2D",

	delegate^self^.gdobj
}

public^let^AnimatedSprite2D = def^{
	self^{ abstract^gdobj : godot.AnimatedSprite2D },

	override^new = f^obj:godot.AnimatedSprite2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimatedSprite2D",

	delegate^self^.gdobj
}

public^let^Area2D = def^{
	self^{ abstract^gdobj : godot.Area2D },

	override^new = f^obj:godot.Area2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Area2D",

	delegate^self^.gdobj
}

public^let^AudioListener2D = def^{
	self^{ abstract^gdobj : godot.AudioListener2D },

	override^new = f^obj:godot.AudioListener2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioListener2D",

	delegate^self^.gdobj
}

public^let^AudioStreamPlayer2D = def^{
	self^{ abstract^gdobj : godot.AudioStreamPlayer2D },

	override^new = f^obj:godot.AudioStreamPlayer2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamPlayer2D",

	delegate^self^.gdobj
}

public^let^BackBufferCopy = def^{
	self^{ abstract^gdobj : godot.BackBufferCopy },

	override^new = f^obj:godot.BackBufferCopy {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BackBufferCopy",

	delegate^self^.gdobj
}

public^let^Bone2D = def^{
	self^{ abstract^gdobj : godot.Bone2D },

	override^new = f^obj:godot.Bone2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Bone2D",

	delegate^self^.gdobj
}

public^let^CPUParticles2D = def^{
	self^{ abstract^gdobj : godot.CPUParticles2D },

	override^new = f^obj:godot.CPUParticles2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CPUParticles2D",

	delegate^self^.gdobj
}

public^let^Camera2D = def^{
	self^{ abstract^gdobj : godot.Camera2D },

	override^new = f^obj:godot.Camera2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Camera2D",

	delegate^self^.gdobj
}

public^let^CanvasGroup = def^{
	self^{ abstract^gdobj : godot.CanvasGroup },

	override^new = f^obj:godot.CanvasGroup {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasGroup",

	delegate^self^.gdobj
}

public^let^CanvasModulate = def^{
	self^{ abstract^gdobj : godot.CanvasModulate },

	override^new = f^obj:godot.CanvasModulate {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasModulate",

	delegate^self^.gdobj
}

public^let^CharacterBody2D = def^{
	self^{ abstract^gdobj : godot.CharacterBody2D },

	override^new = f^obj:godot.CharacterBody2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CharacterBody2D",

	delegate^self^.gdobj
}

public^let^CollisionPolygon2D = def^{
	self^{ abstract^gdobj : godot.CollisionPolygon2D },

	override^new = f^obj:godot.CollisionPolygon2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionPolygon2D",

	delegate^self^.gdobj
}

public^let^CollisionShape2D = def^{
	self^{ abstract^gdobj : godot.CollisionShape2D },

	override^new = f^obj:godot.CollisionShape2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CollisionShape2D",

	delegate^self^.gdobj
}

public^let^Joint2D = def^{
	self^{ abstract^gdobj : godot.Joint2D },

	override^new = f^obj:godot.Joint2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Joint2D",

	delegate^self^.gdobj
}

public^let^DampedSpringJoint2D = def^{
	self^{ abstract^gdobj : godot.DampedSpringJoint2D },

	override^new = f^obj:godot.DampedSpringJoint2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "DampedSpringJoint2D",

	delegate^self^.gdobj
}

public^let^Light2D = def^{
	self^{ abstract^gdobj : godot.Light2D },

	override^new = f^obj:godot.Light2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Light2D",

	delegate^self^.gdobj
}

public^let^DirectionalLight2D = def^{
	self^{ abstract^gdobj : godot.DirectionalLight2D },

	override^new = f^obj:godot.DirectionalLight2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "DirectionalLight2D",

	delegate^self^.gdobj
}

public^let^GPUParticles2D = def^{
	self^{ abstract^gdobj : godot.GPUParticles2D },

	override^new = f^obj:godot.GPUParticles2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GPUParticles2D",

	delegate^self^.gdobj
}

public^let^GrooveJoint2D = def^{
	self^{ abstract^gdobj : godot.GrooveJoint2D },

	override^new = f^obj:godot.GrooveJoint2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GrooveJoint2D",

	delegate^self^.gdobj
}

public^let^LightOccluder2D = def^{
	self^{ abstract^gdobj : godot.LightOccluder2D },

	override^new = f^obj:godot.LightOccluder2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LightOccluder2D",

	delegate^self^.gdobj
}

public^let^Line2D = def^{
	self^{ abstract^gdobj : godot.Line2D },

	override^new = f^obj:godot.Line2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Line2D",

	delegate^self^.gdobj
}

public^let^Marker2D = def^{
	self^{ abstract^gdobj : godot.Marker2D },

	override^new = f^obj:godot.Marker2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Marker2D",

	delegate^self^.gdobj
}

public^let^MeshInstance2D = def^{
	self^{ abstract^gdobj : godot.MeshInstance2D },

	override^new = f^obj:godot.MeshInstance2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MeshInstance2D",

	delegate^self^.gdobj
}

public^let^MultiMeshInstance2D = def^{
	self^{ abstract^gdobj : godot.MultiMeshInstance2D },

	override^new = f^obj:godot.MultiMeshInstance2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MultiMeshInstance2D",

	delegate^self^.gdobj
}

public^let^NavigationLink2D = def^{
	self^{ abstract^gdobj : godot.NavigationLink2D },

	override^new = f^obj:godot.NavigationLink2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationLink2D",

	delegate^self^.gdobj
}

public^let^NavigationObstacle2D = def^{
	self^{ abstract^gdobj : godot.NavigationObstacle2D },

	override^new = f^obj:godot.NavigationObstacle2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationObstacle2D",

	delegate^self^.gdobj
}

public^let^NavigationRegion2D = def^{
	self^{ abstract^gdobj : godot.NavigationRegion2D },

	override^new = f^obj:godot.NavigationRegion2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationRegion2D",

	delegate^self^.gdobj
}

public^let^Parallax2D = def^{
	self^{ abstract^gdobj : godot.Parallax2D },

	override^new = f^obj:godot.Parallax2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Parallax2D",

	delegate^self^.gdobj
}

public^let^ParallaxLayer = def^{
	self^{ abstract^gdobj : godot.ParallaxLayer },

	override^new = f^obj:godot.ParallaxLayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ParallaxLayer",

	delegate^self^.gdobj
}

public^let^Path2D = def^{
	self^{ abstract^gdobj : godot.Path2D },

	override^new = f^obj:godot.Path2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Path2D",

	delegate^self^.gdobj
}

public^let^PathFollow2D = def^{
	self^{ abstract^gdobj : godot.PathFollow2D },

	override^new = f^obj:godot.PathFollow2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PathFollow2D",

	delegate^self^.gdobj
}

public^let^RigidBody2D = def^{
	self^{ abstract^gdobj : godot.RigidBody2D },

	override^new = f^obj:godot.RigidBody2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RigidBody2D",

	delegate^self^.gdobj
}

public^let^PhysicalBone2D = def^{
	self^{ abstract^gdobj : godot.PhysicalBone2D },

	override^new = f^obj:godot.PhysicalBone2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PhysicalBone2D",

	delegate^self^.gdobj
}

public^let^PinJoint2D = def^{
	self^{ abstract^gdobj : godot.PinJoint2D },

	override^new = f^obj:godot.PinJoint2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PinJoint2D",

	delegate^self^.gdobj
}

public^let^PointLight2D = def^{
	self^{ abstract^gdobj : godot.PointLight2D },

	override^new = f^obj:godot.PointLight2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PointLight2D",

	delegate^self^.gdobj
}

public^let^Polygon2D = def^{
	self^{ abstract^gdobj : godot.Polygon2D },

	override^new = f^obj:godot.Polygon2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Polygon2D",

	delegate^self^.gdobj
}

public^let^RayCast2D = def^{
	self^{ abstract^gdobj : godot.RayCast2D },

	override^new = f^obj:godot.RayCast2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RayCast2D",

	delegate^self^.gdobj
}

public^let^RemoteTransform2D = def^{
	self^{ abstract^gdobj : godot.RemoteTransform2D },

	override^new = f^obj:godot.RemoteTransform2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RemoteTransform2D",

	delegate^self^.gdobj
}

public^let^ShapeCast2D = def^{
	self^{ abstract^gdobj : godot.ShapeCast2D },

	override^new = f^obj:godot.ShapeCast2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ShapeCast2D",

	delegate^self^.gdobj
}

public^let^Skeleton2D = def^{
	self^{ abstract^gdobj : godot.Skeleton2D },

	override^new = f^obj:godot.Skeleton2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Skeleton2D",

	delegate^self^.gdobj
}

public^let^Sprite2D = def^{
	self^{ abstract^gdobj : godot.Sprite2D },

	override^new = f^obj:godot.Sprite2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Sprite2D",

	delegate^self^.gdobj
}

public^let^TileMap = def^{
	self^{ abstract^gdobj : godot.TileMap },

	override^new = f^obj:godot.TileMap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileMap",

	delegate^self^.gdobj
}

public^let^TileMapLayer = def^{
	self^{ abstract^gdobj : godot.TileMapLayer },

	override^new = f^obj:godot.TileMapLayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TileMapLayer",

	delegate^self^.gdobj
}

public^let^TouchScreenButton = def^{
	self^{ abstract^gdobj : godot.TouchScreenButton },

	override^new = f^obj:godot.TouchScreenButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TouchScreenButton",

	delegate^self^.gdobj
}

public^let^VisibleOnScreenNotifier2D = def^{
	self^{ abstract^gdobj : godot.VisibleOnScreenNotifier2D },

	override^new = f^obj:godot.VisibleOnScreenNotifier2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisibleOnScreenNotifier2D",

	delegate^self^.gdobj
}

public^let^VisibleOnScreenEnabler2D = def^{
	self^{ abstract^gdobj : godot.VisibleOnScreenEnabler2D },

	override^new = f^obj:godot.VisibleOnScreenEnabler2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisibleOnScreenEnabler2D",

	delegate^self^.gdobj
}
