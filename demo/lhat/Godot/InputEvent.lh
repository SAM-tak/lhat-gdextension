# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under InputEvent. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.InputEvent

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^InputEvent = def^{
	self^{ abstract^gdobj : godot.InputEvent },

	override^new = f^obj:godot.InputEvent {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEvent",

	delegate^self^.gdobj
}

public^let^InputEventAction = def^{
	self^{ abstract^gdobj : godot.InputEventAction },

	override^new = f^obj:godot.InputEventAction {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventAction",

	delegate^self^.gdobj
}

public^let^InputEventJoypadButton = def^{
	self^{ abstract^gdobj : godot.InputEventJoypadButton },

	override^new = f^obj:godot.InputEventJoypadButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventJoypadButton",

	delegate^self^.gdobj
}

public^let^InputEventJoypadMotion = def^{
	self^{ abstract^gdobj : godot.InputEventJoypadMotion },

	override^new = f^obj:godot.InputEventJoypadMotion {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventJoypadMotion",

	delegate^self^.gdobj
}

public^let^InputEventFromWindow = def^{
	self^{ abstract^gdobj : godot.InputEventFromWindow },

	override^new = f^obj:godot.InputEventFromWindow {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventFromWindow",

	delegate^self^.gdobj
}

public^let^InputEventWithModifiers = def^{
	self^{ abstract^gdobj : godot.InputEventWithModifiers },

	override^new = f^obj:godot.InputEventWithModifiers {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventWithModifiers",

	delegate^self^.gdobj
}

public^let^InputEventKey = def^{
	self^{ abstract^gdobj : godot.InputEventKey },

	override^new = f^obj:godot.InputEventKey {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventKey",

	delegate^self^.gdobj
}

public^let^InputEventMIDI = def^{
	self^{ abstract^gdobj : godot.InputEventMIDI },

	override^new = f^obj:godot.InputEventMIDI {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventMIDI",

	delegate^self^.gdobj
}

public^let^InputEventGesture = def^{
	self^{ abstract^gdobj : godot.InputEventGesture },

	override^new = f^obj:godot.InputEventGesture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventGesture",

	delegate^self^.gdobj
}

public^let^InputEventMagnifyGesture = def^{
	self^{ abstract^gdobj : godot.InputEventMagnifyGesture },

	override^new = f^obj:godot.InputEventMagnifyGesture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventMagnifyGesture",

	delegate^self^.gdobj
}

public^let^InputEventMouse = def^{
	self^{ abstract^gdobj : godot.InputEventMouse },

	override^new = f^obj:godot.InputEventMouse {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventMouse",

	delegate^self^.gdobj
}

public^let^InputEventMouseButton = def^{
	self^{ abstract^gdobj : godot.InputEventMouseButton },

	override^new = f^obj:godot.InputEventMouseButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventMouseButton",

	delegate^self^.gdobj
}

public^let^InputEventMouseMotion = def^{
	self^{ abstract^gdobj : godot.InputEventMouseMotion },

	override^new = f^obj:godot.InputEventMouseMotion {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventMouseMotion",

	delegate^self^.gdobj
}

public^let^InputEventPanGesture = def^{
	self^{ abstract^gdobj : godot.InputEventPanGesture },

	override^new = f^obj:godot.InputEventPanGesture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventPanGesture",

	delegate^self^.gdobj
}

public^let^InputEventScreenDrag = def^{
	self^{ abstract^gdobj : godot.InputEventScreenDrag },

	override^new = f^obj:godot.InputEventScreenDrag {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventScreenDrag",

	delegate^self^.gdobj
}

public^let^InputEventScreenTouch = def^{
	self^{ abstract^gdobj : godot.InputEventScreenTouch },

	override^new = f^obj:godot.InputEventScreenTouch {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventScreenTouch",

	delegate^self^.gdobj
}

public^let^InputEventShortcut = def^{
	self^{ abstract^gdobj : godot.InputEventShortcut },

	override^new = f^obj:godot.InputEventShortcut {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "InputEventShortcut",

	delegate^self^.gdobj
}
