# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Node. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Node

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^Node = def^{
	self^{ abstract^gdobj : godot.Node },

	override^new = f^obj:godot.Node {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node",

	delegate^self^.gdobj
}

public^let^Viewport = def^{
	self^{ abstract^gdobj : godot.Viewport },

	override^new = f^obj:godot.Viewport {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Viewport",

	delegate^self^.gdobj
}

public^let^Window = def^{
	self^{ abstract^gdobj : godot.Window },

	override^new = f^obj:godot.Window {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Window",

	delegate^self^.gdobj
}

public^let^AcceptDialog = def^{
	self^{ abstract^gdobj : godot.AcceptDialog },

	override^new = f^obj:godot.AcceptDialog {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AcceptDialog",

	delegate^self^.gdobj
}

public^let^AnimationMixer = def^{
	self^{ abstract^gdobj : godot.AnimationMixer },

	override^new = f^obj:godot.AnimationMixer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationMixer",

	delegate^self^.gdobj
}

public^let^AnimationPlayer = def^{
	self^{ abstract^gdobj : godot.AnimationPlayer },

	override^new = f^obj:godot.AnimationPlayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationPlayer",

	delegate^self^.gdobj
}

public^let^AnimationTree = def^{
	self^{ abstract^gdobj : godot.AnimationTree },

	override^new = f^obj:godot.AnimationTree {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimationTree",

	delegate^self^.gdobj
}

public^let^AudioStreamPlayer = def^{
	self^{ abstract^gdobj : godot.AudioStreamPlayer },

	override^new = f^obj:godot.AudioStreamPlayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamPlayer",

	delegate^self^.gdobj
}

public^let^CanvasLayer = def^{
	self^{ abstract^gdobj : godot.CanvasLayer },

	override^new = f^obj:godot.CanvasLayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasLayer",

	delegate^self^.gdobj
}

public^let^ConfirmationDialog = def^{
	self^{ abstract^gdobj : godot.ConfirmationDialog },

	override^new = f^obj:godot.ConfirmationDialog {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ConfirmationDialog",

	delegate^self^.gdobj
}

public^let^FileDialog = def^{
	self^{ abstract^gdobj : godot.FileDialog },

	override^new = f^obj:godot.FileDialog {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FileDialog",

	delegate^self^.gdobj
}

public^let^HTTPRequest = def^{
	self^{ abstract^gdobj : godot.HTTPRequest },

	override^new = f^obj:godot.HTTPRequest {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HTTPRequest",

	delegate^self^.gdobj
}

public^let^MultiplayerSpawner = def^{
	self^{ abstract^gdobj : godot.MultiplayerSpawner },

	override^new = f^obj:godot.MultiplayerSpawner {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MultiplayerSpawner",

	delegate^self^.gdobj
}

public^let^MultiplayerSynchronizer = def^{
	self^{ abstract^gdobj : godot.MultiplayerSynchronizer },

	override^new = f^obj:godot.MultiplayerSynchronizer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MultiplayerSynchronizer",

	delegate^self^.gdobj
}

public^let^NavigationAgent2D = def^{
	self^{ abstract^gdobj : godot.NavigationAgent2D },

	override^new = f^obj:godot.NavigationAgent2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationAgent2D",

	delegate^self^.gdobj
}

public^let^NavigationAgent3D = def^{
	self^{ abstract^gdobj : godot.NavigationAgent3D },

	override^new = f^obj:godot.NavigationAgent3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NavigationAgent3D",

	delegate^self^.gdobj
}

public^let^ParallaxBackground = def^{
	self^{ abstract^gdobj : godot.ParallaxBackground },

	override^new = f^obj:godot.ParallaxBackground {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ParallaxBackground",

	delegate^self^.gdobj
}

public^let^Popup = def^{
	self^{ abstract^gdobj : godot.Popup },

	override^new = f^obj:godot.Popup {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Popup",

	delegate^self^.gdobj
}

public^let^PopupMenu = def^{
	self^{ abstract^gdobj : godot.PopupMenu },

	override^new = f^obj:godot.PopupMenu {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PopupMenu",

	delegate^self^.gdobj
}

public^let^PopupPanel = def^{
	self^{ abstract^gdobj : godot.PopupPanel },

	override^new = f^obj:godot.PopupPanel {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PopupPanel",

	delegate^self^.gdobj
}

public^let^ResourcePreloader = def^{
	self^{ abstract^gdobj : godot.ResourcePreloader },

	override^new = f^obj:godot.ResourcePreloader {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ResourcePreloader",

	delegate^self^.gdobj
}

public^let^ShaderGlobalsOverride = def^{
	self^{ abstract^gdobj : godot.ShaderGlobalsOverride },

	override^new = f^obj:godot.ShaderGlobalsOverride {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ShaderGlobalsOverride",

	delegate^self^.gdobj
}

public^let^StatusIndicator = def^{
	self^{ abstract^gdobj : godot.StatusIndicator },

	override^new = f^obj:godot.StatusIndicator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "StatusIndicator",

	delegate^self^.gdobj
}

public^let^SubViewport = def^{
	self^{ abstract^gdobj : godot.SubViewport },

	override^new = f^obj:godot.SubViewport {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SubViewport",

	delegate^self^.gdobj
}

public^let^Timer = def^{
	self^{ abstract^gdobj : godot.Timer },

	override^new = f^obj:godot.Timer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Timer",

	delegate^self^.gdobj
}

public^let^WorldEnvironment = def^{
	self^{ abstract^gdobj : godot.WorldEnvironment },

	override^new = f^obj:godot.WorldEnvironment {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "WorldEnvironment",

	delegate^self^.gdobj
}
