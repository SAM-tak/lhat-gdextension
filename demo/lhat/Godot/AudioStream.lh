# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under AudioStream. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.AudioStream

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^AudioStream = def^{
	self^{ abstract^gdobj : godot.AudioStream },

	override^new = f^obj:godot.AudioStream {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStream",

	delegate^self^.gdobj
}

public^let^AudioStreamGenerator = def^{
	self^{ abstract^gdobj : godot.AudioStreamGenerator },

	override^new = f^obj:godot.AudioStreamGenerator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamGenerator",

	delegate^self^.gdobj
}

public^let^AudioStreamInteractive = def^{
	self^{ abstract^gdobj : godot.AudioStreamInteractive },

	override^new = f^obj:godot.AudioStreamInteractive {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamInteractive",

	delegate^self^.gdobj
}

public^let^AudioStreamMP3 = def^{
	self^{ abstract^gdobj : godot.AudioStreamMP3 },

	override^new = f^obj:godot.AudioStreamMP3 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamMP3",

	delegate^self^.gdobj
}

public^let^AudioStreamMicrophone = def^{
	self^{ abstract^gdobj : godot.AudioStreamMicrophone },

	override^new = f^obj:godot.AudioStreamMicrophone {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamMicrophone",

	delegate^self^.gdobj
}

public^let^AudioStreamOggVorbis = def^{
	self^{ abstract^gdobj : godot.AudioStreamOggVorbis },

	override^new = f^obj:godot.AudioStreamOggVorbis {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamOggVorbis",

	delegate^self^.gdobj
}

public^let^AudioStreamPlaylist = def^{
	self^{ abstract^gdobj : godot.AudioStreamPlaylist },

	override^new = f^obj:godot.AudioStreamPlaylist {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamPlaylist",

	delegate^self^.gdobj
}

public^let^AudioStreamPolyphonic = def^{
	self^{ abstract^gdobj : godot.AudioStreamPolyphonic },

	override^new = f^obj:godot.AudioStreamPolyphonic {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamPolyphonic",

	delegate^self^.gdobj
}

public^let^AudioStreamRandomizer = def^{
	self^{ abstract^gdobj : godot.AudioStreamRandomizer },

	override^new = f^obj:godot.AudioStreamRandomizer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamRandomizer",

	delegate^self^.gdobj
}

public^let^AudioStreamSynchronized = def^{
	self^{ abstract^gdobj : godot.AudioStreamSynchronized },

	override^new = f^obj:godot.AudioStreamSynchronized {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamSynchronized",

	delegate^self^.gdobj
}

public^let^AudioStreamWAV = def^{
	self^{ abstract^gdobj : godot.AudioStreamWAV },

	override^new = f^obj:godot.AudioStreamWAV {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioStreamWAV",

	delegate^self^.gdobj
}
