# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under AudioEffect. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.AudioEffect

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^AudioEffect = def^{
	self^{ abstract^gdobj : godot.AudioEffect },

	override^new = f^obj:godot.AudioEffect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffect",

	delegate^self^.gdobj
}

public^let^AudioEffectAmplify = def^{
	self^{ abstract^gdobj : godot.AudioEffectAmplify },

	override^new = f^obj:godot.AudioEffectAmplify {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectAmplify",

	delegate^self^.gdobj
}

public^let^AudioEffectFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectFilter },

	override^new = f^obj:godot.AudioEffectFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectBandLimitFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectBandLimitFilter },

	override^new = f^obj:godot.AudioEffectBandLimitFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectBandLimitFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectBandPassFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectBandPassFilter },

	override^new = f^obj:godot.AudioEffectBandPassFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectBandPassFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectCapture = def^{
	self^{ abstract^gdobj : godot.AudioEffectCapture },

	override^new = f^obj:godot.AudioEffectCapture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectCapture",

	delegate^self^.gdobj
}

public^let^AudioEffectChorus = def^{
	self^{ abstract^gdobj : godot.AudioEffectChorus },

	override^new = f^obj:godot.AudioEffectChorus {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectChorus",

	delegate^self^.gdobj
}

public^let^AudioEffectCompressor = def^{
	self^{ abstract^gdobj : godot.AudioEffectCompressor },

	override^new = f^obj:godot.AudioEffectCompressor {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectCompressor",

	delegate^self^.gdobj
}

public^let^AudioEffectDelay = def^{
	self^{ abstract^gdobj : godot.AudioEffectDelay },

	override^new = f^obj:godot.AudioEffectDelay {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectDelay",

	delegate^self^.gdobj
}

public^let^AudioEffectDistortion = def^{
	self^{ abstract^gdobj : godot.AudioEffectDistortion },

	override^new = f^obj:godot.AudioEffectDistortion {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectDistortion",

	delegate^self^.gdobj
}

public^let^AudioEffectEQ = def^{
	self^{ abstract^gdobj : godot.AudioEffectEQ },

	override^new = f^obj:godot.AudioEffectEQ {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectEQ",

	delegate^self^.gdobj
}

public^let^AudioEffectEQ10 = def^{
	self^{ abstract^gdobj : godot.AudioEffectEQ10 },

	override^new = f^obj:godot.AudioEffectEQ10 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectEQ10",

	delegate^self^.gdobj
}

public^let^AudioEffectEQ21 = def^{
	self^{ abstract^gdobj : godot.AudioEffectEQ21 },

	override^new = f^obj:godot.AudioEffectEQ21 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectEQ21",

	delegate^self^.gdobj
}

public^let^AudioEffectEQ6 = def^{
	self^{ abstract^gdobj : godot.AudioEffectEQ6 },

	override^new = f^obj:godot.AudioEffectEQ6 {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectEQ6",

	delegate^self^.gdobj
}

public^let^AudioEffectHardLimiter = def^{
	self^{ abstract^gdobj : godot.AudioEffectHardLimiter },

	override^new = f^obj:godot.AudioEffectHardLimiter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectHardLimiter",

	delegate^self^.gdobj
}

public^let^AudioEffectHighPassFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectHighPassFilter },

	override^new = f^obj:godot.AudioEffectHighPassFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectHighPassFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectHighShelfFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectHighShelfFilter },

	override^new = f^obj:godot.AudioEffectHighShelfFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectHighShelfFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectLimiter = def^{
	self^{ abstract^gdobj : godot.AudioEffectLimiter },

	override^new = f^obj:godot.AudioEffectLimiter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectLimiter",

	delegate^self^.gdobj
}

public^let^AudioEffectLowPassFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectLowPassFilter },

	override^new = f^obj:godot.AudioEffectLowPassFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectLowPassFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectLowShelfFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectLowShelfFilter },

	override^new = f^obj:godot.AudioEffectLowShelfFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectLowShelfFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectNotchFilter = def^{
	self^{ abstract^gdobj : godot.AudioEffectNotchFilter },

	override^new = f^obj:godot.AudioEffectNotchFilter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectNotchFilter",

	delegate^self^.gdobj
}

public^let^AudioEffectPanner = def^{
	self^{ abstract^gdobj : godot.AudioEffectPanner },

	override^new = f^obj:godot.AudioEffectPanner {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectPanner",

	delegate^self^.gdobj
}

public^let^AudioEffectPhaser = def^{
	self^{ abstract^gdobj : godot.AudioEffectPhaser },

	override^new = f^obj:godot.AudioEffectPhaser {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectPhaser",

	delegate^self^.gdobj
}

public^let^AudioEffectPitchShift = def^{
	self^{ abstract^gdobj : godot.AudioEffectPitchShift },

	override^new = f^obj:godot.AudioEffectPitchShift {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectPitchShift",

	delegate^self^.gdobj
}

public^let^AudioEffectRecord = def^{
	self^{ abstract^gdobj : godot.AudioEffectRecord },

	override^new = f^obj:godot.AudioEffectRecord {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectRecord",

	delegate^self^.gdobj
}

public^let^AudioEffectReverb = def^{
	self^{ abstract^gdobj : godot.AudioEffectReverb },

	override^new = f^obj:godot.AudioEffectReverb {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectReverb",

	delegate^self^.gdobj
}

public^let^AudioEffectSpectrumAnalyzer = def^{
	self^{ abstract^gdobj : godot.AudioEffectSpectrumAnalyzer },

	override^new = f^obj:godot.AudioEffectSpectrumAnalyzer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectSpectrumAnalyzer",

	delegate^self^.gdobj
}

public^let^AudioEffectStereoEnhance = def^{
	self^{ abstract^gdobj : godot.AudioEffectStereoEnhance },

	override^new = f^obj:godot.AudioEffectStereoEnhance {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AudioEffectStereoEnhance",

	delegate^self^.gdobj
}
