# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Mesh. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Mesh

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^Mesh = def^{
	self^{ abstract^gdobj : godot.Mesh },

	override^new = f^obj:godot.Mesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Mesh",

	delegate^self^.gdobj
}

public^let^ArrayMesh = def^{
	self^{ abstract^gdobj : godot.ArrayMesh },

	override^new = f^obj:godot.ArrayMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ArrayMesh",

	delegate^self^.gdobj
}

public^let^PrimitiveMesh = def^{
	self^{ abstract^gdobj : godot.PrimitiveMesh },

	override^new = f^obj:godot.PrimitiveMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PrimitiveMesh",

	delegate^self^.gdobj
}

public^let^BoxMesh = def^{
	self^{ abstract^gdobj : godot.BoxMesh },

	override^new = f^obj:godot.BoxMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoxMesh",

	delegate^self^.gdobj
}

public^let^CapsuleMesh = def^{
	self^{ abstract^gdobj : godot.CapsuleMesh },

	override^new = f^obj:godot.CapsuleMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CapsuleMesh",

	delegate^self^.gdobj
}

public^let^CylinderMesh = def^{
	self^{ abstract^gdobj : godot.CylinderMesh },

	override^new = f^obj:godot.CylinderMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CylinderMesh",

	delegate^self^.gdobj
}

public^let^ImmediateMesh = def^{
	self^{ abstract^gdobj : godot.ImmediateMesh },

	override^new = f^obj:godot.ImmediateMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImmediateMesh",

	delegate^self^.gdobj
}

public^let^PlaceholderMesh = def^{
	self^{ abstract^gdobj : godot.PlaceholderMesh },

	override^new = f^obj:godot.PlaceholderMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderMesh",

	delegate^self^.gdobj
}

public^let^PlaneMesh = def^{
	self^{ abstract^gdobj : godot.PlaneMesh },

	override^new = f^obj:godot.PlaneMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaneMesh",

	delegate^self^.gdobj
}

public^let^PointMesh = def^{
	self^{ abstract^gdobj : godot.PointMesh },

	override^new = f^obj:godot.PointMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PointMesh",

	delegate^self^.gdobj
}

public^let^PrismMesh = def^{
	self^{ abstract^gdobj : godot.PrismMesh },

	override^new = f^obj:godot.PrismMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PrismMesh",

	delegate^self^.gdobj
}

public^let^QuadMesh = def^{
	self^{ abstract^gdobj : godot.QuadMesh },

	override^new = f^obj:godot.QuadMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "QuadMesh",

	delegate^self^.gdobj
}

public^let^RibbonTrailMesh = def^{
	self^{ abstract^gdobj : godot.RibbonTrailMesh },

	override^new = f^obj:godot.RibbonTrailMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RibbonTrailMesh",

	delegate^self^.gdobj
}

public^let^SphereMesh = def^{
	self^{ abstract^gdobj : godot.SphereMesh },

	override^new = f^obj:godot.SphereMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SphereMesh",

	delegate^self^.gdobj
}

public^let^TextMesh = def^{
	self^{ abstract^gdobj : godot.TextMesh },

	override^new = f^obj:godot.TextMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextMesh",

	delegate^self^.gdobj
}

public^let^TorusMesh = def^{
	self^{ abstract^gdobj : godot.TorusMesh },

	override^new = f^obj:godot.TorusMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TorusMesh",

	delegate^self^.gdobj
}

public^let^TubeTrailMesh = def^{
	self^{ abstract^gdobj : godot.TubeTrailMesh },

	override^new = f^obj:godot.TubeTrailMesh {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TubeTrailMesh",

	delegate^self^.gdobj
}
