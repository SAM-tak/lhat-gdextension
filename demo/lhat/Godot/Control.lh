# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Control. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Control

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^Node = def^{
	self^{ abstract^gdobj : godot.Node },

	override^new = f^obj:godot.Node {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node",

	delegate^self^.gdobj
}

public^let^CanvasItem = def^{
	self^{ abstract^gdobj : godot.CanvasItem },

	override^new = f^obj:godot.CanvasItem {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasItem",

	delegate^self^.gdobj
}

public^let^Control = def^{
	self^{ abstract^gdobj : godot.Control },

	override^new = f^obj:godot.Control {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Control",

	delegate^self^.gdobj
}

public^let^Container = def^{
	self^{ abstract^gdobj : godot.Container },

	override^new = f^obj:godot.Container {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Container",

	delegate^self^.gdobj
}

public^let^AspectRatioContainer = def^{
	self^{ abstract^gdobj : godot.AspectRatioContainer },

	override^new = f^obj:godot.AspectRatioContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AspectRatioContainer",

	delegate^self^.gdobj
}

public^let^BaseButton = def^{
	self^{ abstract^gdobj : godot.BaseButton },

	override^new = f^obj:godot.BaseButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BaseButton",

	delegate^self^.gdobj
}

public^let^BoxContainer = def^{
	self^{ abstract^gdobj : godot.BoxContainer },

	override^new = f^obj:godot.BoxContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "BoxContainer",

	delegate^self^.gdobj
}

public^let^Button = def^{
	self^{ abstract^gdobj : godot.Button },

	override^new = f^obj:godot.Button {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Button",

	delegate^self^.gdobj
}

public^let^CenterContainer = def^{
	self^{ abstract^gdobj : godot.CenterContainer },

	override^new = f^obj:godot.CenterContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CenterContainer",

	delegate^self^.gdobj
}

public^let^CheckBox = def^{
	self^{ abstract^gdobj : godot.CheckBox },

	override^new = f^obj:godot.CheckBox {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CheckBox",

	delegate^self^.gdobj
}

public^let^CheckButton = def^{
	self^{ abstract^gdobj : godot.CheckButton },

	override^new = f^obj:godot.CheckButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CheckButton",

	delegate^self^.gdobj
}

public^let^TextEdit = def^{
	self^{ abstract^gdobj : godot.TextEdit },

	override^new = f^obj:godot.TextEdit {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextEdit",

	delegate^self^.gdobj
}

public^let^CodeEdit = def^{
	self^{ abstract^gdobj : godot.CodeEdit },

	override^new = f^obj:godot.CodeEdit {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CodeEdit",

	delegate^self^.gdobj
}

public^let^VBoxContainer = def^{
	self^{ abstract^gdobj : godot.VBoxContainer },

	override^new = f^obj:godot.VBoxContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VBoxContainer",

	delegate^self^.gdobj
}

public^let^ColorPicker = def^{
	self^{ abstract^gdobj : godot.ColorPicker },

	override^new = f^obj:godot.ColorPicker {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ColorPicker",

	delegate^self^.gdobj
}

public^let^ColorPickerButton = def^{
	self^{ abstract^gdobj : godot.ColorPickerButton },

	override^new = f^obj:godot.ColorPickerButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ColorPickerButton",

	delegate^self^.gdobj
}

public^let^ColorRect = def^{
	self^{ abstract^gdobj : godot.ColorRect },

	override^new = f^obj:godot.ColorRect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ColorRect",

	delegate^self^.gdobj
}

public^let^FlowContainer = def^{
	self^{ abstract^gdobj : godot.FlowContainer },

	override^new = f^obj:godot.FlowContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FlowContainer",

	delegate^self^.gdobj
}

public^let^FoldableContainer = def^{
	self^{ abstract^gdobj : godot.FoldableContainer },

	override^new = f^obj:godot.FoldableContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "FoldableContainer",

	delegate^self^.gdobj
}

public^let^GraphEdit = def^{
	self^{ abstract^gdobj : godot.GraphEdit },

	override^new = f^obj:godot.GraphEdit {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GraphEdit",

	delegate^self^.gdobj
}

public^let^GraphElement = def^{
	self^{ abstract^gdobj : godot.GraphElement },

	override^new = f^obj:godot.GraphElement {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GraphElement",

	delegate^self^.gdobj
}

public^let^GraphFrame = def^{
	self^{ abstract^gdobj : godot.GraphFrame },

	override^new = f^obj:godot.GraphFrame {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GraphFrame",

	delegate^self^.gdobj
}

public^let^GraphNode = def^{
	self^{ abstract^gdobj : godot.GraphNode },

	override^new = f^obj:godot.GraphNode {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GraphNode",

	delegate^self^.gdobj
}

public^let^GridContainer = def^{
	self^{ abstract^gdobj : godot.GridContainer },

	override^new = f^obj:godot.GridContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GridContainer",

	delegate^self^.gdobj
}

public^let^HBoxContainer = def^{
	self^{ abstract^gdobj : godot.HBoxContainer },

	override^new = f^obj:godot.HBoxContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HBoxContainer",

	delegate^self^.gdobj
}

public^let^HFlowContainer = def^{
	self^{ abstract^gdobj : godot.HFlowContainer },

	override^new = f^obj:godot.HFlowContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HFlowContainer",

	delegate^self^.gdobj
}

public^let^Range = def^{
	self^{ abstract^gdobj : godot.Range },

	override^new = f^obj:godot.Range {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Range",

	delegate^self^.gdobj
}

public^let^ScrollBar = def^{
	self^{ abstract^gdobj : godot.ScrollBar },

	override^new = f^obj:godot.ScrollBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ScrollBar",

	delegate^self^.gdobj
}

public^let^HScrollBar = def^{
	self^{ abstract^gdobj : godot.HScrollBar },

	override^new = f^obj:godot.HScrollBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HScrollBar",

	delegate^self^.gdobj
}

public^let^Separator = def^{
	self^{ abstract^gdobj : godot.Separator },

	override^new = f^obj:godot.Separator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Separator",

	delegate^self^.gdobj
}

public^let^HSeparator = def^{
	self^{ abstract^gdobj : godot.HSeparator },

	override^new = f^obj:godot.HSeparator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HSeparator",

	delegate^self^.gdobj
}

public^let^Slider = def^{
	self^{ abstract^gdobj : godot.Slider },

	override^new = f^obj:godot.Slider {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Slider",

	delegate^self^.gdobj
}

public^let^HSlider = def^{
	self^{ abstract^gdobj : godot.HSlider },

	override^new = f^obj:godot.HSlider {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HSlider",

	delegate^self^.gdobj
}

public^let^SplitContainer = def^{
	self^{ abstract^gdobj : godot.SplitContainer },

	override^new = f^obj:godot.SplitContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SplitContainer",

	delegate^self^.gdobj
}

public^let^HSplitContainer = def^{
	self^{ abstract^gdobj : godot.HSplitContainer },

	override^new = f^obj:godot.HSplitContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "HSplitContainer",

	delegate^self^.gdobj
}

public^let^ItemList = def^{
	self^{ abstract^gdobj : godot.ItemList },

	override^new = f^obj:godot.ItemList {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ItemList",

	delegate^self^.gdobj
}

public^let^Label = def^{
	self^{ abstract^gdobj : godot.Label },

	override^new = f^obj:godot.Label {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Label",

	delegate^self^.gdobj
}

public^let^LineEdit = def^{
	self^{ abstract^gdobj : godot.LineEdit },

	override^new = f^obj:godot.LineEdit {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LineEdit",

	delegate^self^.gdobj
}

public^let^LinkButton = def^{
	self^{ abstract^gdobj : godot.LinkButton },

	override^new = f^obj:godot.LinkButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "LinkButton",

	delegate^self^.gdobj
}

public^let^MarginContainer = def^{
	self^{ abstract^gdobj : godot.MarginContainer },

	override^new = f^obj:godot.MarginContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MarginContainer",

	delegate^self^.gdobj
}

public^let^MenuBar = def^{
	self^{ abstract^gdobj : godot.MenuBar },

	override^new = f^obj:godot.MenuBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MenuBar",

	delegate^self^.gdobj
}

public^let^MenuButton = def^{
	self^{ abstract^gdobj : godot.MenuButton },

	override^new = f^obj:godot.MenuButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MenuButton",

	delegate^self^.gdobj
}

public^let^NinePatchRect = def^{
	self^{ abstract^gdobj : godot.NinePatchRect },

	override^new = f^obj:godot.NinePatchRect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NinePatchRect",

	delegate^self^.gdobj
}

public^let^OptionButton = def^{
	self^{ abstract^gdobj : godot.OptionButton },

	override^new = f^obj:godot.OptionButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "OptionButton",

	delegate^self^.gdobj
}

public^let^Panel = def^{
	self^{ abstract^gdobj : godot.Panel },

	override^new = f^obj:godot.Panel {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Panel",

	delegate^self^.gdobj
}

public^let^PanelContainer = def^{
	self^{ abstract^gdobj : godot.PanelContainer },

	override^new = f^obj:godot.PanelContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PanelContainer",

	delegate^self^.gdobj
}

public^let^ProgressBar = def^{
	self^{ abstract^gdobj : godot.ProgressBar },

	override^new = f^obj:godot.ProgressBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ProgressBar",

	delegate^self^.gdobj
}

public^let^ReferenceRect = def^{
	self^{ abstract^gdobj : godot.ReferenceRect },

	override^new = f^obj:godot.ReferenceRect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ReferenceRect",

	delegate^self^.gdobj
}

public^let^RichTextLabel = def^{
	self^{ abstract^gdobj : godot.RichTextLabel },

	override^new = f^obj:godot.RichTextLabel {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RichTextLabel",

	delegate^self^.gdobj
}

public^let^ScrollContainer = def^{
	self^{ abstract^gdobj : godot.ScrollContainer },

	override^new = f^obj:godot.ScrollContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ScrollContainer",

	delegate^self^.gdobj
}

public^let^SpinBox = def^{
	self^{ abstract^gdobj : godot.SpinBox },

	override^new = f^obj:godot.SpinBox {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SpinBox",

	delegate^self^.gdobj
}

public^let^SubViewportContainer = def^{
	self^{ abstract^gdobj : godot.SubViewportContainer },

	override^new = f^obj:godot.SubViewportContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "SubViewportContainer",

	delegate^self^.gdobj
}

public^let^TabBar = def^{
	self^{ abstract^gdobj : godot.TabBar },

	override^new = f^obj:godot.TabBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TabBar",

	delegate^self^.gdobj
}

public^let^TabContainer = def^{
	self^{ abstract^gdobj : godot.TabContainer },

	override^new = f^obj:godot.TabContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TabContainer",

	delegate^self^.gdobj
}

public^let^TextureButton = def^{
	self^{ abstract^gdobj : godot.TextureButton },

	override^new = f^obj:godot.TextureButton {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureButton",

	delegate^self^.gdobj
}

public^let^TextureProgressBar = def^{
	self^{ abstract^gdobj : godot.TextureProgressBar },

	override^new = f^obj:godot.TextureProgressBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureProgressBar",

	delegate^self^.gdobj
}

public^let^TextureRect = def^{
	self^{ abstract^gdobj : godot.TextureRect },

	override^new = f^obj:godot.TextureRect {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureRect",

	delegate^self^.gdobj
}

public^let^Tree = def^{
	self^{ abstract^gdobj : godot.Tree },

	override^new = f^obj:godot.Tree {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Tree",

	delegate^self^.gdobj
}

public^let^VFlowContainer = def^{
	self^{ abstract^gdobj : godot.VFlowContainer },

	override^new = f^obj:godot.VFlowContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VFlowContainer",

	delegate^self^.gdobj
}

public^let^VScrollBar = def^{
	self^{ abstract^gdobj : godot.VScrollBar },

	override^new = f^obj:godot.VScrollBar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VScrollBar",

	delegate^self^.gdobj
}

public^let^VSeparator = def^{
	self^{ abstract^gdobj : godot.VSeparator },

	override^new = f^obj:godot.VSeparator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VSeparator",

	delegate^self^.gdobj
}

public^let^VSlider = def^{
	self^{ abstract^gdobj : godot.VSlider },

	override^new = f^obj:godot.VSlider {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VSlider",

	delegate^self^.gdobj
}

public^let^VSplitContainer = def^{
	self^{ abstract^gdobj : godot.VSplitContainer },

	override^new = f^obj:godot.VSplitContainer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VSplitContainer",

	delegate^self^.gdobj
}

public^let^VideoStreamPlayer = def^{
	self^{ abstract^gdobj : godot.VideoStreamPlayer },

	override^new = f^obj:godot.VideoStreamPlayer {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VideoStreamPlayer",

	delegate^self^.gdobj
}
