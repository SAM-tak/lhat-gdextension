# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under Texture. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.Texture

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^Texture = def^{
	self^{ abstract^gdobj : godot.Texture },

	override^new = f^obj:godot.Texture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture",

	delegate^self^.gdobj
}

public^let^Texture2D = def^{
	self^{ abstract^gdobj : godot.Texture2D },

	override^new = f^obj:godot.Texture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture2D",

	delegate^self^.gdobj
}

public^let^AnimatedTexture = def^{
	self^{ abstract^gdobj : godot.AnimatedTexture },

	override^new = f^obj:godot.AnimatedTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AnimatedTexture",

	delegate^self^.gdobj
}

public^let^AtlasTexture = def^{
	self^{ abstract^gdobj : godot.AtlasTexture },

	override^new = f^obj:godot.AtlasTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "AtlasTexture",

	delegate^self^.gdobj
}

public^let^CameraTexture = def^{
	self^{ abstract^gdobj : godot.CameraTexture },

	override^new = f^obj:godot.CameraTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CameraTexture",

	delegate^self^.gdobj
}

public^let^CanvasTexture = def^{
	self^{ abstract^gdobj : godot.CanvasTexture },

	override^new = f^obj:godot.CanvasTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasTexture",

	delegate^self^.gdobj
}

public^let^TextureLayered = def^{
	self^{ abstract^gdobj : godot.TextureLayered },

	override^new = f^obj:godot.TextureLayered {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureLayered",

	delegate^self^.gdobj
}

public^let^CompressedTextureLayered = def^{
	self^{ abstract^gdobj : godot.CompressedTextureLayered },

	override^new = f^obj:godot.CompressedTextureLayered {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedTextureLayered",

	delegate^self^.gdobj
}

public^let^CompressedCubemap = def^{
	self^{ abstract^gdobj : godot.CompressedCubemap },

	override^new = f^obj:godot.CompressedCubemap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedCubemap",

	delegate^self^.gdobj
}

public^let^CompressedCubemapArray = def^{
	self^{ abstract^gdobj : godot.CompressedCubemapArray },

	override^new = f^obj:godot.CompressedCubemapArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedCubemapArray",

	delegate^self^.gdobj
}

public^let^CompressedTexture2D = def^{
	self^{ abstract^gdobj : godot.CompressedTexture2D },

	override^new = f^obj:godot.CompressedTexture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedTexture2D",

	delegate^self^.gdobj
}

public^let^CompressedTexture2DArray = def^{
	self^{ abstract^gdobj : godot.CompressedTexture2DArray },

	override^new = f^obj:godot.CompressedTexture2DArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedTexture2DArray",

	delegate^self^.gdobj
}

public^let^Texture3D = def^{
	self^{ abstract^gdobj : godot.Texture3D },

	override^new = f^obj:godot.Texture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture3D",

	delegate^self^.gdobj
}

public^let^CompressedTexture3D = def^{
	self^{ abstract^gdobj : godot.CompressedTexture3D },

	override^new = f^obj:godot.CompressedTexture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CompressedTexture3D",

	delegate^self^.gdobj
}

public^let^ImageTextureLayered = def^{
	self^{ abstract^gdobj : godot.ImageTextureLayered },

	override^new = f^obj:godot.ImageTextureLayered {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImageTextureLayered",

	delegate^self^.gdobj
}

public^let^Cubemap = def^{
	self^{ abstract^gdobj : godot.Cubemap },

	override^new = f^obj:godot.Cubemap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Cubemap",

	delegate^self^.gdobj
}

public^let^CubemapArray = def^{
	self^{ abstract^gdobj : godot.CubemapArray },

	override^new = f^obj:godot.CubemapArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CubemapArray",

	delegate^self^.gdobj
}

public^let^CurveTexture = def^{
	self^{ abstract^gdobj : godot.CurveTexture },

	override^new = f^obj:godot.CurveTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CurveTexture",

	delegate^self^.gdobj
}

public^let^CurveXYZTexture = def^{
	self^{ abstract^gdobj : godot.CurveXYZTexture },

	override^new = f^obj:godot.CurveXYZTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CurveXYZTexture",

	delegate^self^.gdobj
}

public^let^DPITexture = def^{
	self^{ abstract^gdobj : godot.DPITexture },

	override^new = f^obj:godot.DPITexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "DPITexture",

	delegate^self^.gdobj
}

public^let^ExternalTexture = def^{
	self^{ abstract^gdobj : godot.ExternalTexture },

	override^new = f^obj:godot.ExternalTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ExternalTexture",

	delegate^self^.gdobj
}

public^let^GradientTexture1D = def^{
	self^{ abstract^gdobj : godot.GradientTexture1D },

	override^new = f^obj:godot.GradientTexture1D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GradientTexture1D",

	delegate^self^.gdobj
}

public^let^GradientTexture2D = def^{
	self^{ abstract^gdobj : godot.GradientTexture2D },

	override^new = f^obj:godot.GradientTexture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "GradientTexture2D",

	delegate^self^.gdobj
}

public^let^ImageTexture = def^{
	self^{ abstract^gdobj : godot.ImageTexture },

	override^new = f^obj:godot.ImageTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImageTexture",

	delegate^self^.gdobj
}

public^let^ImageTexture3D = def^{
	self^{ abstract^gdobj : godot.ImageTexture3D },

	override^new = f^obj:godot.ImageTexture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ImageTexture3D",

	delegate^self^.gdobj
}

public^let^MeshTexture = def^{
	self^{ abstract^gdobj : godot.MeshTexture },

	override^new = f^obj:godot.MeshTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "MeshTexture",

	delegate^self^.gdobj
}

public^let^NoiseTexture2D = def^{
	self^{ abstract^gdobj : godot.NoiseTexture2D },

	override^new = f^obj:godot.NoiseTexture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NoiseTexture2D",

	delegate^self^.gdobj
}

public^let^NoiseTexture3D = def^{
	self^{ abstract^gdobj : godot.NoiseTexture3D },

	override^new = f^obj:godot.NoiseTexture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "NoiseTexture3D",

	delegate^self^.gdobj
}

public^let^PlaceholderTextureLayered = def^{
	self^{ abstract^gdobj : godot.PlaceholderTextureLayered },

	override^new = f^obj:godot.PlaceholderTextureLayered {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderTextureLayered",

	delegate^self^.gdobj
}

public^let^PlaceholderCubemap = def^{
	self^{ abstract^gdobj : godot.PlaceholderCubemap },

	override^new = f^obj:godot.PlaceholderCubemap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderCubemap",

	delegate^self^.gdobj
}

public^let^PlaceholderCubemapArray = def^{
	self^{ abstract^gdobj : godot.PlaceholderCubemapArray },

	override^new = f^obj:godot.PlaceholderCubemapArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderCubemapArray",

	delegate^self^.gdobj
}

public^let^PlaceholderTexture2D = def^{
	self^{ abstract^gdobj : godot.PlaceholderTexture2D },

	override^new = f^obj:godot.PlaceholderTexture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderTexture2D",

	delegate^self^.gdobj
}

public^let^PlaceholderTexture2DArray = def^{
	self^{ abstract^gdobj : godot.PlaceholderTexture2DArray },

	override^new = f^obj:godot.PlaceholderTexture2DArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderTexture2DArray",

	delegate^self^.gdobj
}

public^let^PlaceholderTexture3D = def^{
	self^{ abstract^gdobj : godot.PlaceholderTexture3D },

	override^new = f^obj:godot.PlaceholderTexture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PlaceholderTexture3D",

	delegate^self^.gdobj
}

public^let^PortableCompressedTexture2D = def^{
	self^{ abstract^gdobj : godot.PortableCompressedTexture2D },

	override^new = f^obj:godot.PortableCompressedTexture2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "PortableCompressedTexture2D",

	delegate^self^.gdobj
}

public^let^Texture2DArray = def^{
	self^{ abstract^gdobj : godot.Texture2DArray },

	override^new = f^obj:godot.Texture2DArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture2DArray",

	delegate^self^.gdobj
}

public^let^TextureLayeredRD = def^{
	self^{ abstract^gdobj : godot.TextureLayeredRD },

	override^new = f^obj:godot.TextureLayeredRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureLayeredRD",

	delegate^self^.gdobj
}

public^let^Texture2DArrayRD = def^{
	self^{ abstract^gdobj : godot.Texture2DArrayRD },

	override^new = f^obj:godot.Texture2DArrayRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture2DArrayRD",

	delegate^self^.gdobj
}

public^let^Texture2DRD = def^{
	self^{ abstract^gdobj : godot.Texture2DRD },

	override^new = f^obj:godot.Texture2DRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture2DRD",

	delegate^self^.gdobj
}

public^let^Texture3DRD = def^{
	self^{ abstract^gdobj : godot.Texture3DRD },

	override^new = f^obj:godot.Texture3DRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Texture3DRD",

	delegate^self^.gdobj
}

public^let^TextureCubemapArrayRD = def^{
	self^{ abstract^gdobj : godot.TextureCubemapArrayRD },

	override^new = f^obj:godot.TextureCubemapArrayRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureCubemapArrayRD",

	delegate^self^.gdobj
}

public^let^TextureCubemapRD = def^{
	self^{ abstract^gdobj : godot.TextureCubemapRD },

	override^new = f^obj:godot.TextureCubemapRD {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "TextureCubemapRD",

	delegate^self^.gdobj
}

public^let^ViewportTexture = def^{
	self^{ abstract^gdobj : godot.ViewportTexture },

	override^new = f^obj:godot.ViewportTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "ViewportTexture",

	delegate^self^.gdobj
}
