# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# The wrappers under VisualShaderNode. One per engine class, and the tree
# itself is on the host side (05 の 8.8改): godot.Sprite2D is
# declared under godot.Node2D and stands wherever one is asked for,
# so nothing here composes -- which is also why it does not matter
# which of these files a wrapper lives in.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot.VisualShaderNode

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^RefCounted = def^{
	self^{ abstract^gdobj : godot.RefCounted },

	override^new = f^obj:godot.RefCounted {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "RefCounted",

	delegate^self^.gdobj
}

public^let^Resource = def^{
	self^{ abstract^gdobj : godot.Resource },

	override^new = f^obj:godot.Resource {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Resource",

	delegate^self^.gdobj
}

public^let^VisualShaderNode = def^{
	self^{ abstract^gdobj : godot.VisualShaderNode },

	override^new = f^obj:godot.VisualShaderNode {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNode",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeBillboard = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeBillboard },

	override^new = f^obj:godot.VisualShaderNodeBillboard {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeBillboard",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeConstant },

	override^new = f^obj:godot.VisualShaderNodeConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeBooleanConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeBooleanConstant },

	override^new = f^obj:godot.VisualShaderNodeBooleanConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeBooleanConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParameter },

	override^new = f^obj:godot.VisualShaderNodeParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeBooleanParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeBooleanParameter },

	override^new = f^obj:godot.VisualShaderNodeBooleanParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeBooleanParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeClamp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeClamp },

	override^new = f^obj:godot.VisualShaderNodeClamp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeClamp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeColorConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeColorConstant },

	override^new = f^obj:godot.VisualShaderNodeColorConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeColorConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeColorFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeColorFunc },

	override^new = f^obj:godot.VisualShaderNodeColorFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeColorFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeColorOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeColorOp },

	override^new = f^obj:godot.VisualShaderNodeColorOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeColorOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeColorParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeColorParameter },

	override^new = f^obj:godot.VisualShaderNodeColorParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeColorParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeResizableBase = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeResizableBase },

	override^new = f^obj:godot.VisualShaderNodeResizableBase {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeResizableBase",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFrame = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFrame },

	override^new = f^obj:godot.VisualShaderNodeFrame {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFrame",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeComment = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeComment },

	override^new = f^obj:godot.VisualShaderNodeComment {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeComment",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCompare = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCompare },

	override^new = f^obj:godot.VisualShaderNodeCompare {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCompare",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCubemap = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCubemap },

	override^new = f^obj:godot.VisualShaderNodeCubemap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCubemap",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTextureParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTextureParameter },

	override^new = f^obj:godot.VisualShaderNodeTextureParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTextureParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCubemapParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCubemapParameter },

	override^new = f^obj:godot.VisualShaderNodeCubemapParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCubemapParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCurveTexture = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCurveTexture },

	override^new = f^obj:godot.VisualShaderNodeCurveTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCurveTexture",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCurveXYZTexture = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCurveXYZTexture },

	override^new = f^obj:godot.VisualShaderNodeCurveXYZTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCurveXYZTexture",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeCustom = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeCustom },

	override^new = f^obj:godot.VisualShaderNodeCustom {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeCustom",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeDerivativeFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeDerivativeFunc },

	override^new = f^obj:godot.VisualShaderNodeDerivativeFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeDerivativeFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeDeterminant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeDeterminant },

	override^new = f^obj:godot.VisualShaderNodeDeterminant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeDeterminant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeDistanceFade = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeDistanceFade },

	override^new = f^obj:godot.VisualShaderNodeDistanceFade {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeDistanceFade",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeDotProduct = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeDotProduct },

	override^new = f^obj:godot.VisualShaderNodeDotProduct {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeDotProduct",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeGroupBase = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeGroupBase },

	override^new = f^obj:godot.VisualShaderNodeGroupBase {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeGroupBase",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeExpression = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeExpression },

	override^new = f^obj:godot.VisualShaderNodeExpression {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeExpression",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorBase = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorBase },

	override^new = f^obj:godot.VisualShaderNodeVectorBase {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorBase",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFaceForward = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFaceForward },

	override^new = f^obj:godot.VisualShaderNodeFaceForward {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFaceForward",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFloatConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFloatConstant },

	override^new = f^obj:godot.VisualShaderNodeFloatConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFloatConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFloatFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFloatFunc },

	override^new = f^obj:godot.VisualShaderNodeFloatFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFloatFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFloatOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFloatOp },

	override^new = f^obj:godot.VisualShaderNodeFloatOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFloatOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFloatParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFloatParameter },

	override^new = f^obj:godot.VisualShaderNodeFloatParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFloatParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeFresnel = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeFresnel },

	override^new = f^obj:godot.VisualShaderNodeFresnel {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeFresnel",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeGlobalExpression = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeGlobalExpression },

	override^new = f^obj:godot.VisualShaderNodeGlobalExpression {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeGlobalExpression",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIf = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIf },

	override^new = f^obj:godot.VisualShaderNodeIf {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIf",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeInput = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeInput },

	override^new = f^obj:godot.VisualShaderNodeInput {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeInput",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIntConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIntConstant },

	override^new = f^obj:godot.VisualShaderNodeIntConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIntConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIntFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIntFunc },

	override^new = f^obj:godot.VisualShaderNodeIntFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIntFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIntOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIntOp },

	override^new = f^obj:godot.VisualShaderNodeIntOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIntOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIntParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIntParameter },

	override^new = f^obj:godot.VisualShaderNodeIntParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIntParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeIs = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeIs },

	override^new = f^obj:godot.VisualShaderNodeIs {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeIs",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeLinearSceneDepth = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeLinearSceneDepth },

	override^new = f^obj:godot.VisualShaderNodeLinearSceneDepth {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeLinearSceneDepth",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeMix = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeMix },

	override^new = f^obj:godot.VisualShaderNodeMix {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeMix",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeMultiplyAdd = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeMultiplyAdd },

	override^new = f^obj:godot.VisualShaderNodeMultiplyAdd {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeMultiplyAdd",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeOuterProduct = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeOuterProduct },

	override^new = f^obj:godot.VisualShaderNodeOuterProduct {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeOuterProduct",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParameterRef = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParameterRef },

	override^new = f^obj:godot.VisualShaderNodeParameterRef {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParameterRef",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleAccelerator = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleAccelerator },

	override^new = f^obj:godot.VisualShaderNodeParticleAccelerator {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleAccelerator",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleEmitter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleEmitter },

	override^new = f^obj:godot.VisualShaderNodeParticleEmitter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleEmitter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleBoxEmitter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleBoxEmitter },

	override^new = f^obj:godot.VisualShaderNodeParticleBoxEmitter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleBoxEmitter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleConeVelocity = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleConeVelocity },

	override^new = f^obj:godot.VisualShaderNodeParticleConeVelocity {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleConeVelocity",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleEmit = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleEmit },

	override^new = f^obj:godot.VisualShaderNodeParticleEmit {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleEmit",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleMeshEmitter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleMeshEmitter },

	override^new = f^obj:godot.VisualShaderNodeParticleMeshEmitter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleMeshEmitter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleMultiplyByAxisAngle = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleMultiplyByAxisAngle },

	override^new = f^obj:godot.VisualShaderNodeParticleMultiplyByAxisAngle {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleMultiplyByAxisAngle",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeOutput = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeOutput },

	override^new = f^obj:godot.VisualShaderNodeOutput {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeOutput",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleOutput = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleOutput },

	override^new = f^obj:godot.VisualShaderNodeParticleOutput {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleOutput",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleRandomness = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleRandomness },

	override^new = f^obj:godot.VisualShaderNodeParticleRandomness {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleRandomness",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleRingEmitter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleRingEmitter },

	override^new = f^obj:godot.VisualShaderNodeParticleRingEmitter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleRingEmitter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeParticleSphereEmitter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeParticleSphereEmitter },

	override^new = f^obj:godot.VisualShaderNodeParticleSphereEmitter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeParticleSphereEmitter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeProximityFade = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeProximityFade },

	override^new = f^obj:godot.VisualShaderNodeProximityFade {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeProximityFade",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeRandomRange = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeRandomRange },

	override^new = f^obj:godot.VisualShaderNodeRandomRange {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeRandomRange",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeRemap = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeRemap },

	override^new = f^obj:godot.VisualShaderNodeRemap {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeRemap",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeReroute = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeReroute },

	override^new = f^obj:godot.VisualShaderNodeReroute {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeReroute",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeRotationByAxis = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeRotationByAxis },

	override^new = f^obj:godot.VisualShaderNodeRotationByAxis {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeRotationByAxis",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeSDFRaymarch = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeSDFRaymarch },

	override^new = f^obj:godot.VisualShaderNodeSDFRaymarch {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeSDFRaymarch",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeSDFToScreenUV = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeSDFToScreenUV },

	override^new = f^obj:godot.VisualShaderNodeSDFToScreenUV {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeSDFToScreenUV",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeScreenNormalWorldSpace = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeScreenNormalWorldSpace },

	override^new = f^obj:godot.VisualShaderNodeScreenNormalWorldSpace {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeScreenNormalWorldSpace",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeScreenUVToSDF = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeScreenUVToSDF },

	override^new = f^obj:godot.VisualShaderNodeScreenUVToSDF {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeScreenUVToSDF",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeSmoothStep = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeSmoothStep },

	override^new = f^obj:godot.VisualShaderNodeSmoothStep {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeSmoothStep",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeStep = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeStep },

	override^new = f^obj:godot.VisualShaderNodeStep {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeStep",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeSwitch = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeSwitch },

	override^new = f^obj:godot.VisualShaderNodeSwitch {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeSwitch",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture },

	override^new = f^obj:godot.VisualShaderNodeTexture {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeSample3D = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeSample3D },

	override^new = f^obj:godot.VisualShaderNodeSample3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeSample3D",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture2DArray = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture2DArray },

	override^new = f^obj:godot.VisualShaderNodeTexture2DArray {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture2DArray",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture2DArrayParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture2DArrayParameter },

	override^new = f^obj:godot.VisualShaderNodeTexture2DArrayParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture2DArrayParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture2DParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture2DParameter },

	override^new = f^obj:godot.VisualShaderNodeTexture2DParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture2DParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture3D = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture3D },

	override^new = f^obj:godot.VisualShaderNodeTexture3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture3D",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTexture3DParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTexture3DParameter },

	override^new = f^obj:godot.VisualShaderNodeTexture3DParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTexture3DParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTextureParameterTriplanar = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTextureParameterTriplanar },

	override^new = f^obj:godot.VisualShaderNodeTextureParameterTriplanar {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTextureParameterTriplanar",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTextureSDF = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTextureSDF },

	override^new = f^obj:godot.VisualShaderNodeTextureSDF {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTextureSDF",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTextureSDFNormal = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTextureSDFNormal },

	override^new = f^obj:godot.VisualShaderNodeTextureSDFNormal {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTextureSDFNormal",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformCompose = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformCompose },

	override^new = f^obj:godot.VisualShaderNodeTransformCompose {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformCompose",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformConstant },

	override^new = f^obj:godot.VisualShaderNodeTransformConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformDecompose = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformDecompose },

	override^new = f^obj:godot.VisualShaderNodeTransformDecompose {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformDecompose",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformFunc },

	override^new = f^obj:godot.VisualShaderNodeTransformFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformOp },

	override^new = f^obj:godot.VisualShaderNodeTransformOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformParameter },

	override^new = f^obj:godot.VisualShaderNodeTransformParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeTransformVecMult = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeTransformVecMult },

	override^new = f^obj:godot.VisualShaderNodeTransformVecMult {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeTransformVecMult",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUIntConstant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUIntConstant },

	override^new = f^obj:godot.VisualShaderNodeUIntConstant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUIntConstant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUIntFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUIntFunc },

	override^new = f^obj:godot.VisualShaderNodeUIntFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUIntFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUIntOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUIntOp },

	override^new = f^obj:godot.VisualShaderNodeUIntOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUIntOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUIntParameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUIntParameter },

	override^new = f^obj:godot.VisualShaderNodeUIntParameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUIntParameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUVFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUVFunc },

	override^new = f^obj:godot.VisualShaderNodeUVFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUVFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeUVPolarCoord = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeUVPolarCoord },

	override^new = f^obj:godot.VisualShaderNodeUVPolarCoord {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeUVPolarCoord",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVarying = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVarying },

	override^new = f^obj:godot.VisualShaderNodeVarying {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVarying",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVaryingGetter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVaryingGetter },

	override^new = f^obj:godot.VisualShaderNodeVaryingGetter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVaryingGetter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVaryingSetter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVaryingSetter },

	override^new = f^obj:godot.VisualShaderNodeVaryingSetter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVaryingSetter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec2Constant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec2Constant },

	override^new = f^obj:godot.VisualShaderNodeVec2Constant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec2Constant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec2Parameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec2Parameter },

	override^new = f^obj:godot.VisualShaderNodeVec2Parameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec2Parameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec3Constant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec3Constant },

	override^new = f^obj:godot.VisualShaderNodeVec3Constant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec3Constant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec3Parameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec3Parameter },

	override^new = f^obj:godot.VisualShaderNodeVec3Parameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec3Parameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec4Constant = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec4Constant },

	override^new = f^obj:godot.VisualShaderNodeVec4Constant {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec4Constant",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVec4Parameter = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVec4Parameter },

	override^new = f^obj:godot.VisualShaderNodeVec4Parameter {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVec4Parameter",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorCompose = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorCompose },

	override^new = f^obj:godot.VisualShaderNodeVectorCompose {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorCompose",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorDecompose = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorDecompose },

	override^new = f^obj:godot.VisualShaderNodeVectorDecompose {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorDecompose",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorDistance = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorDistance },

	override^new = f^obj:godot.VisualShaderNodeVectorDistance {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorDistance",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorFunc = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorFunc },

	override^new = f^obj:godot.VisualShaderNodeVectorFunc {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorFunc",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorLen = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorLen },

	override^new = f^obj:godot.VisualShaderNodeVectorLen {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorLen",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorOp = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorOp },

	override^new = f^obj:godot.VisualShaderNodeVectorOp {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorOp",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeVectorRefract = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeVectorRefract },

	override^new = f^obj:godot.VisualShaderNodeVectorRefract {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeVectorRefract",

	delegate^self^.gdobj
}

public^let^VisualShaderNodeWorldPositionFromDepth = def^{
	self^{ abstract^gdobj : godot.VisualShaderNodeWorldPositionFromDepth },

	override^new = f^obj:godot.VisualShaderNodeWorldPositionFromDepth {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "VisualShaderNodeWorldPositionFromDepth",

	delegate^self^.gdobj
}
