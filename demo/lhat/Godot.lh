# L^ (lhat) -- GENERATED by scripts/gen-godot-api.py. Do not edit.
#
# One wrapper per engine class. The tree itself is on the host side
# (05 の 8.8改): godot.Sprite2D is declared under godot.Node2D and
# stands wherever one is asked for, so nothing here composes.
#
# delegate^ (02 の 14.7改2) is what shows the class's members
# through the wrapper. It makes no forwarding procedures at all --
# the name is looked for through the handle -- so this file costs a
# few lines per class however many methods that class has.
#
# The members are spelt as the engine spells them: set_rotation, not
# setRotation. What a script writes is what Godot's own
# documentation says.
#
# From Godot Engine v4.5.stable.official.

module^lhat.Godot

import^godot

public^let^Object = def^{
	self^{ abstract^gdobj : godot.Object },

	override^new = f^obj:godot.Object {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Object",

	delegate^self^.gdobj
}

public^let^Node = def^{
	self^{ abstract^gdobj : godot.Node },

	override^new = f^obj:godot.Node {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node",

	delegate^self^.gdobj
}

public^let^CanvasItem = def^{
	self^{ abstract^gdobj : godot.CanvasItem },

	override^new = f^obj:godot.CanvasItem {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "CanvasItem",

	delegate^self^.gdobj
}

public^let^Node2D = def^{
	self^{ abstract^gdobj : godot.Node2D },

	override^new = f^obj:godot.Node2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node2D",

	delegate^self^.gdobj
}

public^let^Sprite2D = def^{
	self^{ abstract^gdobj : godot.Sprite2D },

	override^new = f^obj:godot.Sprite2D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Sprite2D",

	delegate^self^.gdobj
}

public^let^Node3D = def^{
	self^{ abstract^gdobj : godot.Node3D },

	override^new = f^obj:godot.Node3D {
		self^{ gdobj = obj }
	},

	# 02 の 18: what the engine registers this class as.
	gdBaseClass = "Node3D",

	delegate^self^.gdobj
}
